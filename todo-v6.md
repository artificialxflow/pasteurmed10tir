# TODO v6 — SMS + Zohal + Go-Live prep

سایت: `https://pasteur.plus`  
تاریخ شروع برنامه: ۱۴۰۵ / ۲۰۲۶-۰۸  
پیش‌نیاز انجام‌شده:

- [x] پترن‌های هاست ایران / payamak-panel تأیید شده‌اند
- [x] Body IDها در `.env.local` و Runflare ست شده‌اند
- [x] `ZOHAL_TOKEN` / `ZOHAL_BASE_URL` در env ست شده‌اند
- [x] `DEV_OTP_*` هنوز برای تست نگه داشته شود تا SMS پایدار شود

**نام‌گذاری:** این فایل جدا از `backend-dev/TODO-v5.md` (باشگاه/فاز ۵) است.

مستندات پنل SMS: https://docs.payamak-panel.com/  
Base REST پیشنهادی: `https://rest.payamak-panel.com`

---

## Body ID مرجع (پترن‌های تأیید‌شده)

| کاربرد | env | bodyId | متغیرها |
|--------|-----|--------|---------|
| OTP ورود | `SMS_OTP_BODY_ID` | `514428` | `{0}` = کد |
| یادآور ۲۴س / فردا | `SMS_REMINDER_24H_BODY_ID` | `514430` | `{0}` زمان، `{1}` خدمت |
| یادآور ۲ ساعت | `SMS_REMINDER_2H_BODY_ID` | `514431` | `{0}` زمان، `{1}` خدمت |
| تأیید رزرو | `SMS_BOOKING_BODY_ID` | `514432` | `{0}` زمان، `{1}` خدمت |
| ثبت مشاوره | `SMS_CONSULTATION_BODY_ID` | `514436` | `{0}` کد پیگیری |

سایر env لازم:

```text
SMS_USERNAME=
SMS_API_KEY=          # کلید API وب‌سرویس (نه لزوماً پسورد لاگین)
SMS_BASE_URL=https://rest.payamak-panel.com
CRON_SECRET=
ZOHAL_TOKEN=
ZOHAL_BASE_URL=https://service.zohal.io/api/v0/services
```

Zohal و SMS تداخل ندارند؛ ترتیب اجرا عمداً SMS اول است.

---

## ترتیب اجرای پیشنهادی

```text
A (کلاینت SMS)
  → B (OTP لاگین)
  → C (مشاوره / رزرو SMS)
  → D (یادآور نوبت)
  → E (Zohal)
  → F (آماده‌سازی Go-Live)
```

خارج از این todo (عمداً نه در v6): وبلاگ، درگاه پرداخت واقعی کامل، اپ اندروید.

---

# فاز A — زیرساخت SMS (پایه)

- [x] ماژول `lib/sms/` (کلاینت payamak-panel / REST)
- [x] خواندن env: `SMS_USERNAME`, `SMS_API_KEY`, `SMS_BASE_URL`, همه `SMS_*_BODY_ID`
- [x] متد عمومی `sendByPattern(bodyId, to, vars[])`
- [x] مدیریت خطا / timeout؛ لاگ بدون لو دادن API key
- [x] تشخیص «SMS پیکربندی شده است» (همهٔ ضروری‌ها موجود باشند)
- [x] تست دستی یک ارسال OTP به شماره واقعی روی staging / local با env واقعی (2026-08-09 → 09214843361؛ کاربر تأیید کرد پیامک رسید)

**خروجی فاز A:** امکان ارسال الگو از سرور بدون اتصال کامل به UI.

---

# فاز B — ورود واقعی با OTP (اولویت ۱)

- [x] وصل `POST /api/auth/otp/send` به پترن OTP (`514428`)
- [x] تولید کد امن، ذخیره موقت با TTL، محدودیت تعداد ارسال / cooldown
- [x] منطق شفاف با `DEV_OTP_*`:
  - شماره/کد تست در صورت وجود env همچنان کار کند، **یا**
  - اگر SMS fail شد فقط مسیر DEV فعال بماند (مستند شود)
- [x] `POST /api/auth/otp/verify` با کد پیامکی واقعی
- [ ] تست لایو: شماره واقعی → دریافت SMS → ورود موفق
- [ ] تست رگرسیون: `09126723365` / `00000` وقتی DEV هنوز فعال است
- [ ] Deploy روی Runflare + تأیید وجود env روی سرور
- [ ] Migration `006_sms_zohal` روی DB (`prisma migrate deploy` — از این ماشین فعلاً P1001)

**خروجی فاز B:** بیمار با پیامک واقعی وارد می‌شود؛ تست DEV نشکسته است.

---

# فاز C — پیامک‌های تراکنشی

- [x] بعد از ثبت مشاوره موفق → پترن `514436` (`{0}` = کد/شناسه پیگیری)
- [x] بعد از رزرو / پرداخت موفق → پترن `514432` (`{0}` زمان، `{1}` خدمت)
- [x] خطای SMS نباید کل تراکنش موفق را rollback کند؛ لاگ + قابل‌مشاهده برای ادمین/لاگ سرور
- [x] جلوگیری از اسپم در صورت ری‌تری کاربر

**خروجی فاز C:** مشاوره و رزرو SMS اطلاع‌رسانی می‌فرستند.

---

# فاز D — یادآور نوبت

- [x] یادآور ۲۴ ساعت قبل با پترن `514430`
- [x] یادآور ۲ ساعت قبل با پترن `514431`
- [x] منبع: رزروهای پرداخت‌شده / تأییدشده با زمان مشخص
- [x] جلوگیری از ارسال تکراری (فلگ/رکورد در DB)
- [x] مکانیزم زمان‌بندی: cron روی سرور، endpoint امن زمان‌دار، یا اسکریپت زمان‌بندی‌شده Runflare
- [ ] تست با یک نوبت واقعی نزدیک یا شبیه‌سازی زمان
- [ ] ست کردن `CRON_SECRET` + زمان‌بندی روی Runflare

**خروجی فاز D:** یادآورها خودکار ارسال می‌شوند.

---

# فاز E — Zohal

- [x] ماژول `lib/zohal/` با `ZOHAL_TOKEN` و `ZOHAL_BASE_URL`
- [x] کد ملی در پنل کاربری **اجباری** + اعتبارسنجی الگوریتم ۱۰ رقمی
- [x] شاهکار (تطبیق موبایل ↔ کد ملی) قبل از عملیات حساس
- [x] استعلام اطلاعات هویتی در صورت نیاز فلو
- [x] روی درخواست تسهیلات: **اعتبارسنجی** + **چک برگشتی**
- [x] ذخیره نتیجه استعلام (وضعیت، زمان، خلاصه) برای ادمین
- [ ] تست با شماره/کد ملی مجاز تست یا محیط واقعی با احتیاط هزینه استعلام

سرویس‌های توصیه‌شده توکن (یادآوری):

- شاهکار
- استعلام اطلاعات هویتی
- استعلام اعتبار سنجی
- استعلام چک برگشتی

**خروجی فاز E:** تسهیلات بدون کد ملی / استعلام رد می‌شود؛ نتایج در پنل دیده می‌شود.

---

# فاز F — آماده‌سازی Go-Live (هنوز لانچ عمومی کامل نیست)

- [ ] چند دور تست موفق SMS واقعی روی لایو
- [x] تصمیم: حذف کامل `DEV_OTP_*` یا محدود کردن فقط به یک شماره داخلی → **فعلاً نگه داشتن DEV تا SMS پایدار**
- [x] اجرای موارد مرتبط از `backend-dev/GO-LIVE.md` (چک‌لیست به‌روز؛ حذف DEV وقتی آماده)
- [x] به‌روزرسانی `MANUAL-SMOKE-CHECKLIST.md` / `TEST-MANUAL.md` برای ورود با SMS واقعی
- [x] اطمینان: توکن SMS/Zohal در گیت یا فایل‌های `updates/` نباشد (توکن از `updates/04/01` پاک شد)

**خروجی فاز F:** آماده ورود به لانچ عمومی از نظر OTP (درگاه پرداخت می‌تواند فاز بعدی جدا باشد).

---

## وضعیت پیشرفت (دستی پر شود)

| فاز | وضعیت | تاریخ | نکته |
|-----|--------|--------|------|
| A SMS lib | ✅ done | 2026-08-09 | SMS واقعی به 09214843361 OK |
| B OTP login | ✅ code done | 2026-08-09 | migrate + لایو + deploy باقی |
| C transactional SMS | ✅ done | 2026-08-09 | وابسته به SMS config روی سرور |
| D reminders | ✅ code done | 2026-08-09 | نیاز به CRON_SECRET + schedule |
| E Zohal | ✅ code done | 2026-08-09 | تست واقعی استعلام باقی |
| F Go-Live prep | 🟨 docs ready | 2026-08-09 | DEV_OTP عمداً نگه داشته شده |

---

## ایمنی

- API key و توکن را در چت / اسکرین / commit نگذارید.
- اگر توکنی قبلاً در `updates/` لو رفت، در پنل باطل و توکن جدید بگیرید.
- روی لایو اسکن/تست سنگین هم‌زمان با ارسال انبوه SMS نکنید.
- هزینه هر استعلام Zohal و هر پیامک را در تست لحاظ کنید.
