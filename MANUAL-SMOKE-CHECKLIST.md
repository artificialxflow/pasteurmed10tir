# چک‌لیست تست دستی — Pasteur Plus

سایت: `https://pasteur.plus`  
هدف: یک دور کامل smoke دستی برای تأیید staging قبل از اتوماسیون / Go-Live.

هر مورد را با ✅ یا ❌ علامت بزنید. در صورت خطا یک خط توضیح بنویسید.

---

## اطلاعات ورود

| نقش | مسیر | داده |
|-----|------|------|
| بیمار | منو **پنل کاربری** → `/account` | موبایل `09126723365` → کد `00000` |
| ادمین کامل | منو **پنل ادمین** → `/admin/login` | `admin` + رمز از `ADMIN-CREDENTIALS.local.md` |
| اپراتور محدود | `/admin/login` | `ops` + رمز از همان فایل |

کدهای معرف (اختیاری): `PLUS100` ، `PLUS200` ، `EQUIPVIP`

**نکته:** قبل از شروع، Incognito یا پنجره بدون لاگین قبلی باز کنید. اول بیمار را تست کنید؛ برای ادمین tab/پنجره جدا بگیرید.

**Env لازم روی سرور (نباید حذف شود تا پایان تست staging):**

```
DEV_OTP_PHONE=09126723365
DEV_OTP_CODE=00000
```

---

## ترتیب پیشنهادی (حدود ۳۰–۴۵ دقیقه)

```text
ورود بیمار → رزرو → ادمین رزروها → مشاوره → ادمین مشاوره‌ها
→ باشگاه (مشاهده + مسواک + کول‌داون) → ادمین باشگاه
→ یکی از مالی (عضویت یا فروشگاه یا کیف)
→ خروج ادمین → ورود ops → رزروها OK → /admin/access ممنوع
→ پر کردن گزارش انتهای فایل
```

---

# بخش ۱ — ورود بیمار

### ۱.۱ ورود

1. بروید به: `https://pasteur.plus/account`  
   یا منوی بالا → **پنل کاربری**
2. شماره: `09126723365`
3. دریافت کد
4. کد: `00000` (+ نام در صورت نیاز)
5. وارد شوید

**انتظار:** وارد پنل کاربری می‌شوید؛ صفحه خطا نمی‌دهد.

- [ ] OK
- [ ] FAIL — توضیح: _______________

### ۱.۲ ماندن لاگین

1. همان صفحه را Refresh کنید (F5)

**انتظار:** هنوز لاگین هستید.

- [ ] OK
- [ ] FAIL — توضیح: _______________

### ۱.۳ پروفایل + زحل (v7)

1. در `/account` کد ملی معتبر و بیمه/فرانشیز را ذخیره کنید
2. اگر `ZOHAL_TOKEN` روی سرور فعال است: badge «شاهکار: تطبیق» و وضعیت «تأیید شده» بدون کلیک ادمین
3. اگر زحل غیرفعال: وضعیت «در بررسی» → ادمین `/admin/patients` تأیید دستی

**انتظار:** پروفایل read-only برای تأییدشده؛ ستون «زحل» در `/admin/patients`

- [ ] OK
- [ ] FAIL — توضیح: _______________

---

# بخش ۱ب — استعلام بیمه رزرو (walkthrough v7)

### ۱ب.۱ تأیید کاربری

1. بیمار `/account` — پروفایل کامل
2. ادمین `/admin/patients` — وضعیت **تأیید شده** (یا زحل خودکار)

- [ ] OK
- [ ] FAIL — توضیح: _______________

### ۱ب.۲ استعلام در `/dental/confirm`

1. رزرو تا صفحه تأیید نهایی (`/dental/confirm`)
2. نوع بیمه را انتخاب کنید → «استعلام و بررسی کارشناسان»
3. دکمه «بروزرسانی وضعیت کاربری» را در صورت نیاز بزنید

**انتظار:** درخواست در `/admin/insurances` → «درخواست‌های استعلام» ظاهر شود

- [ ] OK
- [ ] FAIL — توضیح: _______________

### ۱ب.۳ تأیید استعلام + مبلغ

1. ادمین `/admin/insurances` — استعلام را **تأیید** کنید
2. بیمار F5 روی `/dental/confirm` یا polling ۱۵s

**انتظار:** مبلغ = `350000 × franchise%` (مثلاً ۳۰٪ → ۱۰۵٬۰۰۰ تومان)

- [ ] OK
- [ ] FAIL — توضیح: _______________

### ۱ب.۴ CRUD پزشکان (ادمین)

1. `/admin/doctors` — تب **دندانپزشکان**
2. یک دندانپزشک را ویرایش کنید (نام/روز) → **ذخیره تغییرات**
3. `/dental/general` — نام جدید در لیست دیده شود
4. تب **متخصصین پزشکی** — ویرایش یک متخصص → ذخیره → `/medical/doctors`

**انتظار:** لیست سایت از API/DB می‌آید (نه hardcode).

- [ ] OK
- [ ] FAIL — توضیح: _______________

---

### ۲.۱ ثبت رزرو (بیمار)

1. منوی بالا → **دندانپزشکی**  
   یا مستقیم: `https://pasteur.plus/dental/booking`
2. سرویس / زمان / پزشک را انتخاب کنید
3. در صورت فیلد معرف: `PLUS100` (اختیاری)
4. تا پایان بروید و **پرداخت mock** را بزنید
5. پیام موفقیت / صفحه success را ببینید

**یادداشت:** ساعت / سرویس / کد رزرو: _______________

- [ ] OK
- [ ] FAIL — توضیح: _______________

### ۲.۲ تأیید در ادمین

1. پنجره جدا → `https://pasteur.plus/admin/login`
2. ورود با `admin` و رمز از `ADMIN-CREDENTIALS.local.md`
3. منوی ادمین → **رزروها** (`/admin/bookings`)
4. رزرو تازه را با موبایل `09126723365` یا ساعت پیدا کنید

**انتظار:** یک ردیف جدید برای رزرو شما هست.

- [ ] OK
- [ ] FAIL — توضیح: _______________

---

# بخش ۳ — مشاوره → چک ادمین

### ۳.۱ ثبت مشاوره (بیمار)

1. منوی بالا → **مشاوره و ویزیت**  
   یا `https://pasteur.plus/consultation`
2. فرم را با داده ساختگی پر کنید، مثلاً:
   - نام: `تست کاربر`
   - موضوع/توضیح: `تست دستی مشاوره`
3. ارسال کنید

- [ ] OK
- [ ] FAIL — توضیح: _______________

### ۳.۲ تأیید در ادمین

1. با همان ادمین → منو **مشاوره‌ها** (`/admin/consultations`)
2. درخواست تازه را پیدا کنید

- [ ] OK
- [ ] FAIL — توضیح: _______________

---

# بخش ۴ — باشگاه مشتریان

### ۴.۱ مشاهده امتیاز

1. منوی بالا → **باشگاه**  
   یا `https://pasteur.plus/club`
2. در صورت نیاز شماره: `09126723365` → **مشاهده امتیاز**
3. امتیاز فعلی و تاریخچه را یادداشت کنید

**امتیاز قبل از مسواک:** _______________

- [ ] OK
- [ ] FAIL — توضیح: _______________

### ۴.۲ مسواک زدم

1. روی **مسواک زدم** بزنید

**انتظار:** حدود +۵ امتیاز؛ شمارش امروز زیاد می‌شود.

**امتیاز بعد:** _______________

- [ ] OK
- [ ] FAIL — توضیح: _______________

### ۴.۳ کول‌داون

1. بلافاصله دوباره **مسواک زدم** بزنید

**انتظار:** پیام انتظار (~۸ ساعت) یا دکمه غیرفعال.

- [ ] OK
- [ ] FAIL — توضیح: _______________

### ۴.۴ ادمین باشگاه

1. ادمین → منو **باشگاه** (`/admin/club`)
2. داده مربوط به همان شماره/امتیاز را ببینید

- [ ] OK
- [ ] FAIL — توضیح: _______________

---

# بخش ۵ — یکی از مسیرهای مالی (حداقل یکی)

یکی از سه گزینه کافی است؛ اگر وقت دارید هر سه را بزنید.

### ۵.۱ گزینه A — عضویت

1. بیمار: `https://pasteur.plus/dental/membership`
2. یک پلن را انتخاب / پیش‌نمایش باز کنید
3. اگر مودال باز شد، روی پس‌زمینه (بیرون مودال) کلیک کنید → باید بسته شود
4. پرداخت mock را انجام دهید
5. ادمین → **عضویت‌ها** (`/admin/memberships`) → ردیف جدید را چک کنید

- [ ] OK
- [ ] FAIL
- [ ] SKIP — توضیح: _______________

### ۵.۲ گزینه B — فروشگاه

1. منو **تجهیزات** → `https://pasteur.plus/shop` یا `/shop/catalog`
2. یک محصول به سبد → `/shop/cart`
3. سفارش را ثبت / پرداخت mock
4. ادمین → **فروشگاه** (`/admin/shop`) → سفارش را ببینید

- [ ] OK
- [ ] FAIL
- [ ] SKIP — توضیح: _______________

### ۵.۳ گزینه C — کیف اعتبار

1. بیمار لاگین → `https://pasteur.plus/wallet`
2. سقف / تراکنش‌ها باید بدون صفحه سفید لود شوند

- [ ] OK
- [ ] FAIL
- [ ] SKIP — توضیح: _______________

---

# بخش ۶ — دسترسی نقش ops (امنیت سبک)

### ۶.۱ لاگین ops

1. از ادمین `admin` خارج شوید
2. `/admin/login` با `ops` و رمز از `ADMIN-CREDENTIALS.local.md`
3. بروید منو **رزروها**

**انتظار:** لیست رزروها را می‌بینید.

- [ ] OK
- [ ] FAIL — توضیح: _______________

### ۶.۲ ممنوع بودن سطح دسترسی

1. مستقیم بروید: `https://pasteur.plus/admin/access`  
   یا اگر لینک **سطح دسترسی** را دیدید روی آن کلیک کنید

**انتظار:** ممنوع / عدم دسترسی / ریدایرکت — نه صفحه مدیریت نقش‌ها.

- [ ] OK
- [ ] FAIL — توضیح: _______________

---

# بخش ۷ — چک سریع ظاهری (اختیاری ~۵ دقیقه)

در Chrome:

1. F12 → تب **Lighthouse**
2. یک‌بار روی صفحه اصلی `/`
3. یک‌بار روی `/account`

- [ ] SKIP
- [ ] انجام شد — نمره موبایل `/`: ____ — نمره `/account`: ____

---

# قالب گزارش نهایی

بعد از اتمام تست، این بخش را پر کنید:

```text
تاریخ/ساعت تست: ……
سایت: https://pasteur.plus

۱.۱ ورود بیمار: OK / FAIL — …
۱.۲ Refresh لاگین: OK / FAIL — …
۲.۱ رزرو بیمار: OK / FAIL — …
۲.۲ رزرو در ادمین: OK / FAIL — …
۳.۱ مشاوره بیمار: OK / FAIL — …
۳.۲ مشاوره در ادمین: OK / FAIL — …
۴.۱ باشگاه مشاهده: OK / FAIL — امتیاز قبل: …
۴.۲ مسواک: OK / FAIL — امتیاز بعد: …
۴.۳ کول‌داون: OK / FAIL — …
۴.۴ باشگاه ادمین: OK / FAIL — …
۵ مالی (A عضویت / B فروشگاه / C کیف): OK / FAIL / SKIP — …
۶.۱ ops → رزروها: OK / FAIL — …
۶.۲ ops → /admin/access ممنوع: OK / FAIL — …
۷ Lighthouse: SKIP / نمره …

مشکلات مهم:
1) …
2) …
```

---

## حساب‌های ادمین

یوزرنیم‌ها: `admin` ، `ops` ، `content` ، `finance`  
رمزها: فقط در فایل خصوصی `ADMIN-CREDENTIALS.local.md` (gitignore) — روی UI یا در Git نیست.

---

## نکات ایمنی

- این چک‌لیست برای **staging / تست روی pasteur.plus با OTP تستی** است.
- روی پروداکشن عمومی بدون محدودیت، Agent یا ابزار خودکار آزاد وصل نکنید.
- تا قبل از Go-Live واقعی، `DEV_OTP_*` را حذف نکنید.
- اسکریپت `reset-all` را روی دیتای واقعی مهم اجرا نکنید مگر عمداً بخواهید همه داده پاک شود.

---

## ورود با SMS واقعی (بعد از اتصال payamak)

وقتی `SMS_*` روی سرور ست است و migration اجرا شده:

1. با شماره‌ای غیر از `DEV_OTP_PHONE` به `/account` بروید → ارسال کد.
2. پیامک با پترن OTP را دریافت کنید و وارد شوید.
3. رگرسیون DEV: هنوز با `09126723365` / `00000` وارد شوید (تا وقتی env تست فعال است).
4. یک مشاوره ثبت کنید → SMS پیگیری؛ یک رزرو → SMS تأیید.
5. یادآور: `POST /api/cron/sms-reminders` با `Authorization: Bearer $CRON_SECRET`.
