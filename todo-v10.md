# TODO v10 — اقساط (`08/01`) · تیکت (`08/02`) · ساعت حضور (`08/03`) · رزرو زمان (`08/04`) · پورسانت ویزیتور (`08/05`)

سایت: `https://pasteur.plus`  
تاریخ ایجاد: ۲۰۲۶-۰۸-۲۴  
مرجع: `updates/08/01/` · `updates/08/02/` · `updates/08/03/` · `updates/08/04/` · `updates/08/05/` · `todo-v9.md` · `todo-v8.md` فاز ۸ · `MANUAL-SMOKE-CHECKLIST.md`

> **هدف v10:**  
> 1. صفحه «اقساط من» از خلاصهٔ طرح به **صورتحساب واقعی** تبدیل شود: لیست قسط‌به‌قسط، معوقه، تاریخچه واریزی، و **پرداخت آنلاین** (زیبال).  
> 2. **تیکت پشتیبانی روی وب بیمار** واقعاً کار کند (ثبت + لیست + follow-up) — الان ادمین هست، وب عملاً خراب/غیرقابل‌استفاده.  
> 3. در ادمین پزشکان، فیلدهای **از — تا** ساعت حضور بعد از خالی/صفر شدن دوباره قابل تایپ باشند (`08/03`).  
> 4. در رزرو نوبت، مرحلهٔ **انتخاب زمان** اسلات‌ها را نشان دهد و دیگر با صفحهٔ خالی + خطای «زمان را انتخاب کنید» گیر نکند (`08/04`).  
> 5. در ادمین ویزیتورها، ویرایش **درصد پورسانت** (بالینی / فروشگاه) بعد از پاک کردن فیلد دوباره ممکن باشد (`08/05`).  
> **ادامه v9 (ops):** فاز ۰ محتوا و فاز ۵ go-live — در انتهای این فایل.

---

## خلاصه وضعیت

| موضوع | الان (پس از v9) | v10 |
|--------|------------------|-----|
| طرح اقساط بعد از approve وام/تسهیلات/اعتبار | ✅ ساخته می‌شود | — |
| `/installments` — کل / پرداخت‌شده / مانده / سررسید بعدی | ✅ صورتحساب | ✅ فاز ۱–۲ |
| لیست قسط‌به‌قسط (۱ از N) | ✅ | ✅ فاز ۱ |
| وضعیت معوقه / پرداخت‌شده / سررسید | ✅ | ✅ فاز ۱ |
| تاریخچه واریزی‌ها | ✅ | ✅ فاز ۲ |
| دکمه «پرداخت این قسط» (درگاه) | ✅ | ✅ فاز ۳ |
| کسر از `paidAmount` / به‌روز صورتحساب بعد از پرداخت | ✅ | ✅ فاز ۳ |
| پرداخت از کیف اعتبار (اختیاری) | ❌ | ⬜ فاز ۴ |
| ثبت دستی واریز توسط ادمین | ✅ | ✅ فاز ۵ |
| اعلان SMS سررسید / معوقه (اختیاری) | ❌ | ⬜ فاز ۶ |
| تیکت بیمار `/support` (ثبت + لیست) | 🟨 کد هست · عملاً خراب | ⬜ فاز ۷ |
| تیکت ادمین `/admin/support` | ✅ | — |
| ادمین پزشکان — تایپ ساعت «از — تا» | ✅ فیکس شد | ✅ فاز ۸ |
| رزرو نوبت — مرحله انتخاب زمان / اسلات | ✅ فیکس شد | ✅ فاز ۹ |
| ادمین ویزیتور — ویرایش ٪ پورسانت | ✅ فیکس شد | ✅ فاز ۱۰ |
| محتوای واقعی مرکز · go-live ops | ⬜ v9 | ⬜ ادامه |

---

## تشخیص `08/01`

**گزارش:** «اقساط را می‌آورد ولی لیست اقساط، معوقه‌ها و واریزی‌ها نیست و جایی برای واریز قسط نیست — کامل کاربری شود.»

| لایه | الان | مشکل |
|------|------|------|
| `InstallmentPlan` | `totalAmount` · `paidAmount` · `dueDates[]` · `installmentCount` | بدون ردیف قسط و بدون رکورد پرداخت |
| `InstallmentsPage` | کارت خلاصه per طرح | بدون جدول قسط / پرداخت |
| درگاه زیبال | رزرو · عضویت · فروشگاه | **kind اقساط ندارد** |
| ادمین `/admin/installments` | لیست طرح‌ها | بدون ثبت واریز دستی / جزئیات قسط |

**الگوی موجود برای کپی:** `PaymentIntent` + `createZibalPaymentIntent` · callback زibal · `createFacilityInstallmentPlan` / `createLoanInstallmentPlan`

**فایل‌های کلیدی:**  
`components/account/InstallmentsPage.tsx` · `lib/commerce/installment-service.ts` · `lib/patient.ts` · `prisma/schema.prisma` (`InstallmentPlan`) · `app/api/commerce/installments/route.ts` · `app/admin/(panel)/installments/page.tsx` · `lib/commerce/zibal-intent-service.ts` · `app/api/payments/zibal/*`

---

## تشخیص `08/02`

**گزارش:** «تیکت گذاشتن در قسمت وب فعلا وجود نداره ولی از ادمین وجود داره — لطفا فیکس کن طوری که کار کنه.»

| لایه | الان | مشکل |
|------|------|------|
| Schema + migration `013` | ✅ `SupportTicket` + `SupportMessage` | — |
| ادمین `/admin/support` | ✅ inbox · پاسخ · بستن | کار می‌کند |
| بیمار `/support` + `/app/support` | 🟨 UI هست | ثبت عملاً می‌خورد به دیوار |
| `POST /api/operations/support/tickets` | نام + موبایل **الزامی** | فرم وب فقط `subject` + `body` می‌فرستد |
| اگر لاگین ولی `user.name` خالی | — | خطا: «نام و موبایل الزامی است» |
| `GET` لیست تیکت‌ها | بدون session → ۴۰۱ | صفحه بلافاصله خطا می‌دهد؛ مسیر مهمان در UI نیست |
| کشف‌پذیری | فقط لینک در `/account` | در منو/فوتر سایت نیست → حس «وجود ندارد» |

**فایل‌های کلیدی:**  
`components/support/SupportPage.tsx` · `app/(site)/support/page.tsx` · `app/app/support/page.tsx` · `app/api/operations/support/tickets/route.ts` · `app/api/operations/support/tickets/[id]/route.ts` · `app/admin/(panel)/support/page.tsx` · `components/account/AccountPage.tsx` · `lib/operations/client.ts`

**نیاز به migration جدید؟** خیر — مگر اینکه بعداً فیلد اضافه شود. فیکس عمدتاً UI + قرارداد API + discoverability است.

---

## تشخیص `08/03`

**گزارش:** «اینجا تایم نمیشه زد وقتی صفر میکنی دیگه تایپ نمیشه و اجازه ورود عدد نمیده — فیکس کن.»  
**عکس:** `updates/08/03/photo_2026-08-24_15-02-43.jpg` — ادمین → پزشکان → افزودن دندانپزشک → «روز و ساعت حضور (جدا برای هر روز)» · فیلدهای از — تا.

| لایه | الان | مشکل |
|------|------|------|
| `DayHoursEditor` در `doctors/page.tsx` | `type="number"` controlled با `number` | خالی کردن → `Number("") === 0` → فوری `0` |
| fallback در `onChange` | اگر غیرfinite → `9` / `17` | فیلد هرگز خالی نمی‌ماند برای تایپ دوباره |
| «تا» با `min={1}` | اگر مقدار `0` شود | روی تبلت/موبایل ورودی قفل یا عجیب می‌شود |
| لیست + فرم افزودن | هر دو از همان editor | باگ در هر دو جا |

**فایل‌های کلیدی:**  
`app/admin/(panel)/doctors/page.tsx` (`DayHoursEditor`) · در صورت نیاز `lib/content/doctor-mappers.ts` (`buildScheduleFromDayHours` / اعتبار بازه)

**نیاز به migration؟** خیر — باگ UI controlled input.

---

## تشخیص `08/04`

**گزارش:** «اینجا هم نمیره مرحله رزرو زمانی نمیاره و انتخاب زمان میاره.»  
**عکس:** `updates/08/04/photo_2026-08-24_15-08-48.jpg` — `/dental/booking` · استپر ۳مرحله‌ای · خطای قرمز «لطفاً زمان را انتخاب کنید» بدون لیست اسلات.

| لایه | الان | مشکل |
|------|------|------|
| ورود با `?doctor=&day=` | steps = `type` → `time` → `info` | روز/پزشک از قبل آمده؛ فقط نوع + زمان |
| زیرعنوان صفحه | همیشه «انتخاب نوع خدمت…» | روی مرحلهٔ زمان هم همان متن → گمراه‌کننده |
| `TimeStep` | `if (!doctor \|\| !day \|\| !type) return null` | صفحهٔ خالی؛ بدون پیام |
| `next()` روی `time` | اگر `timeValue == null` | همان خطا حتی وقتی اسلاتی رندر نشده |
| draft `pendingBooking` | ممکن است step=`time` بدون `type` | TimeStep خالی + خطای زمان |
| `schedule[day].visitHours` / `treatmentSlots` | اگر خالی (مرتبط با `08/03`) | راهنما هست ولی دکمه ساعت نیست / یا حس «نمیاره» |
| پزشک از API | اگر id نامعتبر یا هنوز لود نشده | `doctor` undefined → `return null` |

**فایل‌های کلیدی:**  
`components/dental/BookingWizard.tsx` (`TimeStep` · `next` · steps از query) · `components/dental/DentistList.tsx` (لینک `?doctor=&day=`) · `lib/content/doctor-mappers.ts` / schedule ذخیره‌شده (وابستگی به فاز ۸)

**نیاز به migration؟** خیر — UX + guard + empty state؛ دادهٔ schedule از فاز ۸ سالم شود.

---

## تشخیص `08/05`

**گزارش:** «تو قسمت پورسانت ویزیتورها هم پاک میکنی ویرایش نمیشه پورسانت ویزیتور — فیکس کن.»

همان الگوی باگ `08/03` (controlled `number` + coerce به `0` روی خالی).

| لایه | الان | مشکل |
|------|------|------|
| فرم افزودن ویزیتور | `rateClinical` / `rateShop` به‌صورت **string** | ✅ تایپ آزاد OK |
| جدول ویرایش ردیف | `value={number}` + `Number(e.target.value \|\| 0)` | خالی → فوری `0` → قفل تایپ |
| فیلدها | `commissionRateClinical` · `commissionRateShop` (+ sync `commissionRate`) | هر دو ستون بالینی و فروشگاه |
| صفحه `/admin/commissions` | لیست ledger فقط خواندنی | مربوط نیست؛ باگ روی `/admin/visitors` است |

**فایل‌های کلیدی:**  
`app/admin/(panel)/visitors/page.tsx` (ورودی‌های جدول) · الگوی فیکس مشابه فاز ۸ (`DayHoursEditor`)

**نیاز به migration؟** خیر — فقط UI controlled input در جدول ویرایش.

---

## migrations پیشنهادی

| Migration | موضوع | v10 |
|-----------|--------|-----|
| `018_installment_schedule_payments` | مدل `InstallmentScheduleItem` + `InstallmentPayment` (یا معادل) | ✅ فاز ۰ |
| (اختیاری) | `InstallmentPlan.monthlyAmount` cache | ⬜ |
| — | فاز ۴–۶ · ۷ · ۸ · ۹ · ۱۰ بدون migration | ⬜ |

---

# فاز ۰ — مدل داده و قرارداد محصول (P0) ✅ کد

### ۰.۱ — تصمیم محصول

- [x] مبلغ هر قسط: باقی‌مانده روی آخرین قسط (`floor` + remainder)
- [x] پرداخت جزئی یک قسط: **خیر** — فقط تمام مبلغ مانده قسط
- [x] پرداخت زودتر از سررسید: **مجاز**
- [x] انتخاب آزاد هر قسط باز (نه فقط بعدی)
- [x] منبع پرداخت پیش‌فرض: **زیبال** (+ ثبت دستی ادمین در فاز ۵؛ کیف = فاز ۴ باز)

### ۰.۲ — Schema

- [x] `InstallmentScheduleItem`
- [x] `InstallmentPayment`
- [x] migration `018_installment_schedule_payments` (+ heal/backfill در `listVisibleInstallments` / `ensureScheduleForPlan`)
- [x] هنگام `createLoan` / `createFacility` / `createCredit` → همزمان schedule

### ۰.۳ — API قرارداد

- [x] `GET /api/commerce/installments` — طرح + `items[]` + `payments[]` + `overdueAmount`
- [x] `POST /api/commerce/installments/pay`
- [x] `POST /api/admin/commerce/installments/manual-pay`

**Done when:** مدل و تصمیم‌های محصول قفل شده؛ backfill طرح‌های فعلی ممکن است.

---

# فاز ۱ — UI صورتحساب بیمار: لیست قسط + معوقه (P0) ✅ کد

**گزارش `08/01`:** لیست اقساط و معوقه‌ها نمایش داده نمی‌شود.

### ۱.۱ — `InstallmentsPage`

- [x] per طرح: هدر خلاصه (کل · پرداخت‌شده · مانده · وضعیت طرح)
- [x] جدول/لیست قسط‌ها: شماره · سررسید · مبلغ · وضعیت
- [x] هایلایت معوقه
- [x] جمع معوقه در بالای کارت
- [x] empty state واضح اگر طرح نیست
- [x] نسخه PWA `/app/installments` همان رفتار

### ۱.۲ — منطق وضعیت

- [x] `paid` / `overdue` / `due` / `pending` / `partial` در `deriveItemStatus`
- [x] helper در `lib/commerce/installment-service.ts`

### ۱.۳ — Smoke

- [ ] طرح ۱۲ قسطه → ۱۲ ردیف با تاریخ
- [ ] تاریخ گذشته بدون پرداخت → معوقه
- [ ] `MANUAL-SMOKE-CHECKLIST.md` — بند v10.۱

**Done when:** بیمار بدون ابهام ببیند چند قسط مانده، کدام معوقه است، و هر قسط چقدر است.

---

# فاز ۲ — تاریخچه واریزی‌ها (P0) ✅ کد

**گزارش `08/01`:** واریزی‌ها نشان داده نمی‌شود.

### ۲.۱ — UI بیمار

- [x] بخش «تاریخچه پرداخت» زیر هر طرح
- [x] تاریخ · مبلغ · روش · کد پیگیری
- [x] اگر خالی: «هنوز واریزی ثبت نشده»

### ۲.۲ — Backend

- [x] خواندن `InstallmentPayment` در GET اقساط
- [x] mapper + type در `lib/patient.ts`

### ۲.۳ — Smoke

- [ ] بعد از یک پرداخت موفق، ردیف در تاریخچه ظاهر شود
- [ ] `MANUAL-SMOKE-CHECKLIST.md` — بند v10.۲

**Done when:** هر واریز موفق در صورتحساب بیمار دیده می‌شود.

---

# فاز ۳ — پرداخت آنلاین قسط (زیبال) + کسر از صورتحساب (P0) ✅ کد

**گزارش `08/01`:** جایی برای واریز قسط نیست؛ باید در صورتحساب کسر شود.

### ۳.۱ — درگاه

- [x] `PaymentIntent.payload.kind = 'installment'`
- [x] `POST /api/commerce/installments/pay`
- [x] ساخت intent زیبال · redirect
- [x] callback/verify با trackId · مالک phone
- [x] idempotent: پرداخت مجدد قسط paid رد می‌شود

### ۳.۲ — اعمال روی صورتحساب

- [x] `InstallmentPayment` completed
- [x] افزایش `scheduleItem.paidAmount` / status
- [x] افزایش `InstallmentPlan.paidAmount`
- [x] اگر همه paid → `plan.status = completed`
- [x] success/fail → `/installments?paid=1|0`

### ۳.۳ — UI

- [x] دکمه «پرداخت این قسط»
- [x] غیرفعال برای اقساط پرداخت‌شده
- [x] هر قسط باز قابل پرداخت
- [x] loading / خطا

### ۳.۴ — Smoke

- [ ] پرداخت قسط ۱ → زیبال → بازگشت → paid
- [ ] refresh پایدار
- [ ] پرداخت مجدد همان قسط → خطا
- [ ] `MANUAL-SMOKE-CHECKLIST.md` — بند v10.۳

**Done when:** بیمار از پنل یک قسط را آنلاین پرداخت کند و صورتحساب بلافاصله به‌روز شود.

---

# فاز ۴ — پرداخت از کیف اعتبار (P1 · اختیاری محصول)

> فقط اگر در ۰.۱ تأیید شد.

### ۴.۱ — Backend

- [ ] method `wallet`: کسر از موجودی/سقف مطابق قوانین فعلی کیف (`debit`)
- [ ] اگر موجودی کافی نیست → پیام و پیشنهاد زیبال
- [ ] تراکنش کیف با reason «پرداخت قسط …»

### ۴.۲ — UI

- [ ] انتخاب روش: «درگاه» / «کیف اعتبار»
- [ ] نمایش اعتبار باقی‌مانده

### ۴.۳ — Smoke

- [ ] کیف با اعتبار کافی → قسط paid بدون زیبال
- [ ] کیف ناکافی → خطا · طرح تغییر نکرده

**Done when:** در صورت فعال‌بودن، بیمار بتواند قسط را از کیف بپردازد.

---

# فاز ۵ — ادمین: جزئیات + ثبت دستی واریز (P1) ✅ کد

### ۵.۱ — `/admin/installments`

- [x] باز کردن طرح → لیست اقساط + ثبت واریز دستی
- [x] فرم توضیح اختیاری + دکمه ثبت
- [x] method=`manual` همان منطق فاز ۳
- [x] بنر راهنما: وام ≠ تسهیلات ≠ اعتبار

### ۵.۲ — دسترسی

- [x] permission `installments`

### ۵.۳ — Smoke

- [ ] ادمین واریز دستی → بیمار در تاریخچه می‌بیند
- [ ] `MANUAL-SMOKE-CHECKLIST.md` — بند v10.۵

**Done when:** پذیرش بتواند بدون درگاه، واریز حضوری را روی صورتحساب ثبت کند.

---

# فاز ۶ — یادآوری و polish (P2)

### ۶.۱ — یادآوری (اختیاری)

- [ ] کرون/یادآور: N روز قبل سررسید · یا معوقه
- [ ] SMS با لینک `/installments` یا `/app/installments`
- [ ] هماهنگی با `CRON_SECRET` و قالب‌های SMS موجود

### ۶.۲ — UX / copy

- [ ] تفاوت واضح: «اقساط وام/تسهیلات» vs «سقف کیف اعتبار»
- [ ] لینک از `/wallet` و `/account` به اقساط (اگر کامل نیست تقویت شود)
- [ ] empty state و پیام‌های خطا فارسی و دقیق

### ۶.۳ — مستندات

- [ ] `MANUAL-SMOKE-CHECKLIST.md` — بخش کامل v10
- [ ] `todo-v9.md` — یک خط «ادامه اقساط قابل پرداخت در v10»
- [ ] در صورت نیاز به‌روز `backend-dev/TEST-FULL-WALKTHROUGH-FA.md`

**Done when:** جریان اقساط برای بیمار و پذیرش قابل استفاده روزمره است و smoke ثبت شده.

---

# فاز ۷ — تیکت پشتیبانی وب بیمار (`08/02`) (P0 · موازی با اقساط) ✅ کد

**گزارش:** تیکت روی وب نیست/کار نمی‌کند؛ ادمین هست — باید کار کند.

> این فاز **وابسته به migration اقساط نیست** و می‌تواند موازی با فاز ۰–۳ اجرا شود.  
> **پیاده‌سازی:** `SupportPage` + فوتر + `/help` — ۲۰۲۶-۰۸-۲۴. تصمیم: **فقط لاگین**.

### ۷.۰ — تأیید محیط

- [ ] روی prod: migration `013_support_tickets` اعمال شده (`npx prisma migrate deploy`)
- [ ] `/support` و `/admin/support` در deploy فعلی لود می‌شوند
- [x] اگر جدول نیست → اول migrate؛ بعد UI *(کد آماده؛ ops روی prod)*

### ۷.۱ — تصمیم محصول (سریع)

- [x] ثبت تیکت: **فقط لاگین** (مهمان مجاز نیست)
- [x] لاگین الزامی برای لیست و follow-up؛ CTA ورود به `/account`؛ نام از پروفایل یا فیلد اجباری در فرم

### ۷.۲ — فیکس فرم و API بیمار

- [x] `SupportPage`: قبل از submit، اطمینان از `name` + `phone`
  - از session/پروفایل پر شود
  - اگر نام خالی → فیلد نام در فرم (required)
- [x] `POST` body شامل `name` / `phone` وقتی لازم است (علاوه بر subject/body)
- [x] پیام خطای API واضح و فارسی روی UI
- [x] بعد از ثبت موفق → thread باز + ردیف در لیست
- [x] follow-up روی تیکت باز؛ روی `closed` مسدود

### ۷.۳ — UX لود / لاگین

- [x] اگر `GET` → ۴۰۱: کارت «برای مشاهده و ثبت تیکت وارد شوید» + لینک `/account`
- [ ] بعد از لاگین، برگشت به `/support` (returnUrl — الگوی موجود ندارد؛ کاربر دستی برمی‌گردد)
- [x] empty state فقط وقتی لاگین است و واقعاً تیکتی نیست
- [x] همان رفتار در `/app/support`

### ۷.۴ — کشف‌پذیری (discoverability)

- [x] لینک «پشتیبانی / تیکت» در فوتر سایت
- [x] لینک «پشتیبانی» در منوی بالای سایت + نوار پایین موبایل
- [x] از `/help` لینک به پشتیبانی
- [x] لینک حساب کاربری + کارت‌های واضح اقساط/تیکت در `/account`

### ۷.۵ — ادمین (smoke، نه feature جدید)

- [ ] تیکت ساخته‌شده از وب در `/admin/support` با status باز دیده شود
- [ ] پاسخ ادمین در thread بیمار ظاهر شود
- [ ] بستن تیکت → بیمار نتواند follow-up بفرستد
- [ ] (اختیاری از v8) badge «تیکت باز» روی dashboard — اگر وقت بود

### ۷.۶ — Smoke + docs

- [ ] بیمار لاگین با نام پر → تیکت جدید OK
- [ ] بیمار لاگین بدون نام → فرم نام می‌خواهد → بعد OK
- [ ] ادمین پاسخ → بیمار می‌بیند
- [x] مهمان: مجاز نیست (۷.۱)
- [ ] `MANUAL-SMOKE-CHECKLIST.md` — بند v10.۷
- [x] تیکت وب ✅ (کد)

**Done when:** بیمار از وب بتواند تیکت بسازد، ببیند، follow-up کند؛ ادمین همان thread را ببیند و جواب دهد — بدون وابستگی به «فقط ادمین».

---

# فاز ۸ — تایپ ساعت حضور پزشکان (`08/03`) (P0 · سریع) ✅ کد

**گزارش:** وقتی فیلد ساعت را صفر/خالی می‌کنی دیگر نمی‌شود عدد زد (ادمین → پزشکان → از — تا).

> **پیاده‌سازی:** `DraftNumberInput` + `DayHoursEditor` — ۲۰۲۶-۰۸-۲۴.

### ۸.۱ — ریشه باگ (تأیید)

- [x] در `DayHoursEditor`: `Number("") === 0` باعث snap به صفر می‌شد
- [x] fallback فوری به `9`/`17` روی هر keystroke مانع تایپ بود
- [x] `min={1}` روی «تا» وقتی value=0 تجربه تبلت را خراب می‌کرد

### ۸.۲ — فیکس UI

- [x] حین ویرایش: draft string در `DraftNumberInput` تا blur
- [x] خالی مجاز تا blur — خالی به `0` تبدیل نمی‌شود
- [x] parse عدد روی **blur**
- [x] اعتبارسنجی بازه روی افزودن/ذخیره (`validateDayHours`)
- [x] روز تیک‌خورده با بازه نامعتبر → جلوگیری از save
- [x] همان editor برای فرم «افزودن» و ردیف‌های لیست

### ۸.۳ — سازگاری داده

- [x] `buildScheduleFromDayHours` / `applyDayHoursToDentist` با اعداد commit‌شده
- [x] ذخیره موفق → `schedule` و برچسب `hours`
- [x] روز بدون تیک همچنان `null`

### ۸.۴ — Smoke

- [ ] فیلد «از» را پاک کن → خالی بماند → `۱۷` تایپ شود
- [ ] فیلد «تا» را پاک کن → `۹` سپس اصلاح به `۱۷`
- [ ] صفر میانی بدون قفل کیبورد
- [ ] تست روی **تبلت/موبایل** + دسکتاپ
- [ ] افزودن دندانپزشک جدید + ویرایش ردیف + «ذخیره تغییرات»
- [ ] `MANUAL-SMOKE-CHECKLIST.md` — بند v10.۸

**Done when:** ادمین بتواند ساعت از/تا را آزادانه پاک و دوباره تایپ کند؛ ذخیره با بازه معتبر کار کند.

---

# فاز ۹ — رزرو نوبت: نمایش اسلات زمان (`08/04`) (P0) ✅ کد

**گزارش:** مرحلهٔ رزرو زمان اسلات نمی‌آورد؛ فقط خطای «لطفاً زمان را انتخاب کنید» می‌دهد.

> **پیاده‌سازی:** `BookingWizard` / `TimeStep` — ۲۰۲۶-۰۸-۲۴.

### ۹.۱ — ریشه باگ (تأیید)

- [x] مسیر کوتاه `?doctor=&day=` → ۳ استپ؛ استپر ۲ = `time`
- [x] `TimeStep` دیگر `return null` خام ندارد
- [x] draft بدون `type` → برگشت به `type`
- [x] schedule خالی → empty state
- [x] کپی صفحه بر اساس `currentStep` (`STEP_LABELS`)

### ۹.۲ — Guard و empty state

- [x] پیام‌های جدا: بارگذاری · پزشک یافت نشد · نوع خدمت · روز بدون برنامه / بدون اسلات
- [x] اگر روی `time` و `type` نیست → خودکار برگرد به `type`
- [x] پزشک یافت نشد → لینک برگشت به لیست
- [x] اسلات خالی → empty state واضح نه خطای زمان گمراه‌کننده

### ۹.۳ — اعتبارسنجی `next()` / copy

- [x] روی `time`: اگر اسلاتی نیست → پیام متناسب
- [x] زیرعنوان بر اساس `currentStep`
- [x] app و web یکسان

### ۹.۴ — جریان کامل رزرو

- [x] مسیر با query و بدون query از همان `TimeStep` اسلات نشان می‌دهد
- [x] occupied slots همچنان «پر» را غیرفعال می‌کند
- [x] heal: step=`time` بدون type → برگشت به type

### ۹.۵ — وابستگی داده (فاز ۸)

- [x] فیکس ساعت حضور در فاز ۸ برای ساخت schedule معتبر
- [ ] smoke با دندانپزشک تازه‌ذخیره‌شده از ادمین (دستی پس از deploy)

### ۹.۶ — Smoke

- [ ] موبایل: مسیر `08/04` → اسلات‌ها ظاهر شوند
- [ ] بدون انتخاب زمان → خطا فقط وقتی اسلات هست
- [ ] پزشک بدون schedule → پیام خالی، نه بن‌بست
- [ ] `MANUAL-SMOKE-CHECKLIST.md` — بند v10.۹

**Done when:** بیمار در مرحلهٔ زمان، ساعت‌های قابل رزرو را می‌بیند و انتخاب می‌کند؛ دیگر صفحهٔ خالی + خطای گمراه‌کننده نیست.

---

# فاز ۱۰ — ویرایش پورسانت ویزیتور (`08/05`) (P0 · سریع) ✅ کد

**گزارش:** پاک کردن فیلد پورسانت → دیگر نمی‌شود ویرایش/تایپ کرد.

> **پیاده‌سازی:** `DraftNumberInput` در `/admin/visitors` — ۲۰۲۶-۰۸-۲۴.

### ۱۰.۱ — ریشه باگ (تأیید)

- [x] جدول: `Number(e.target.value || 0)` روی بالینی و فروشگاه
- [x] خالی → `0` فوری
- [x] فرم افزودن از قبل string بود — فقط ردیف‌های لیست خراب بودند

### ۱۰.۲ — فیکس UI

- [x] `DraftNumberInput` برای بالینی و فروشگاه (draft تا blur)
- [x] خالی مجاز تا blur
- [x] اعتبار ۰–۱۰۰ در `saveVisitor` / `validateRate`
- [x] sync `commissionRate` با بالینی هنگام commit/ذخیره
- [x] helper مشترک `components/ui/DraftNumberInput.tsx` با فاز ۸

### ۱۰.۳ — Smoke

- [ ] ستون بالینی را پاک کن → دوباره `۷` تایپ و ذخیره
- [ ] ستون فروشگاه را پاک کن → `۱۰` تایپ و ذخیره
- [ ] مقدار نامعتبر → خطا
- [ ] reload صفحه → درصدها پایدار
- [ ] `MANUAL-SMOKE-CHECKLIST.md` — بند v10.۱۰

**Done when:** ادمین بتواند درصد پورسانت ویزیتور را پاک و دوباره ویرایش و ذخیره کند.

---

# ادامه v9 — ops (خارج از هسته `08/01`–`08/05` ولی باز)

## فاز A — محتوای واقعی (v9 فاز ۰)

- [ ] `/admin/services` · laser · nursing · doctors · insurances · consultation-prices · bookings deposit · visitors
- [ ] smoke مسیر بیمار با داده واقعی مرکز

## فاز B — Go-live ops (v9 فاز ۵)

- [ ] `SESSION_SECRET` · `CRON_SECRET`
- [ ] `ZOHAL_TOKEN` + smoke
- [ ] CRON SMS reminders
- [ ] Zibal production + IP whitelist
- [ ] حذف `DEV_OTP_*`
- [ ] `MANUAL-SMOKE-CHECKLIST.md` full · `KALI-SECURITY-CHECKLIST.md`

---

## ترتیب پیشنهادی اجرا

```text
فاز ۸ (ساعت حضور 08/03)        ← سریع؛ UI ادمین + داده schedule سالم
فاز ۱۰ (پورسانت ویزیتور 08/05) ← سریع؛ همان الگوی number input
فاز ۹ (اسلات رزرو 08/04)        ← بعد/همراه ۸؛ BookingWizard
فاز ۷ (تیکت وب 08/02)          ← موازی؛ بدون migration جدید
  ║
فاز ۰ (مدل اقساط + تصمیم)
  → فاز ۱ (لیست قسط + معوقه)     ← ارزش فوری برای گزارش 08/01
  → فاز ۲ (تاریخچه واریزی)
  → فاز ۳ (پرداخت زیبال)         ← هسته «واریز کند و کسر شود»
  → فاز ۵ (ادمین دستی)
  → فاز ۴ (کیف — اگر خواستند)
  → فاز ۶ (یادآوری + docs)
  → A/B ops از v9
```

---

## Done when (کل v10)

**اقساط (`08/01`)** — بیمار در `/installments` (و PWA) بتواند:

1. لیست کامل اقساط و معوقه‌ها را ببیند  
2. تاریخچه واریزی‌ها را ببیند  
3. قسط باز را آنلاین بپردازد  
4. بلافاصله مانده/وضعیت صورتحساب به‌روز شود  

و پذیرش بتواند در ادمین واریز حضوری ثبت کند.

**تیکت (`08/02`)** — بیمار در `/support` (و `/app/support`) بتواند:

1. تیکت جدید ثبت کند (با نام/موبایل معتبر)  
2. لیست و thread خودش را ببیند  
3. follow-up بفرستد  

و ادمین در `/admin/support` همان مکالمه را ببیند و پاسخ دهد.

**ساعت حضور (`08/03`)** — در `/admin/doctors` ادمین بتواند:

1. فیلدهای از — تا را خالی کند و دوباره عدد بزند (تبلت و دسکتاپ)  
2. بازه معتبر را ذخیره کند بدون قفل شدن ورودی

**رزرو زمان (`08/04`)** — در `/dental/booking` بیمار بتواند:

1. در مرحلهٔ زمان اسلات‌های واقعی را ببیند  
2. زمان را انتخاب کند و به مرحلهٔ بعد برود  
3. اگر اسلاتی نیست، پیام واضح ببیند نه خطای گمراه‌کننده

**پورسانت ویزیتور (`08/05`)** — در `/admin/visitors` ادمین بتواند:

1. درصد بالینی / فروشگاه را پاک و دوباره تایپ کند  
2. با «ذخیره» مقدار معتبر ۰–۱۰۰ ثبت شود