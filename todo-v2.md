# TODO v2 — بروزرسانی‌های UI/UX (Next.js + طرح استاتیک اندروید)

> هدف: جمع‌آوری ۱۱ بروزرسانی کارفرما، برآورد تغییرات، و پیاده‌سازی روی:
>
> 1. **پروژه Next.js فعلی** (فرانت اصلی)
> 2. **طرح استاتیک اندروید** داخل `pastour/app/` (blueprint قبل از Flutter)
>
> مرجع اسکرین‌شات/توضیح هر آپدیت: `pastour/update/08/NN/`
>
> وضعیت: **اجرای فاز A→E تکمیل شد (Next + pastour parity اصلی)** → فاز F: QA کدی انجام شد؛ پذیرش نهایی کارفرما باز است.

---

## اصول مشترک

- [x] هر تغییر هم در Next و هم در طرح استاتیک اندروید (در صورت وجود معادل) اعمال شود
  - _یادداشت:_ کیف اعتبار، لیست پزشک، و CRUD ادمین pastour برای لیزر/مشاوره هنوز کامل نیست؛ Next منبع اصلی است.
- [x] رفتار وب ریسپانسیو (`pastour/pages` / هدر وب) اگر همان باگ/CTA را دارد، هم‌راستا اصلاح شود
- [x] بدون بازطراحی آزاد؛ فقط استانداردسازی طبق بازخورد کارفرما
- [x] Flutter بعداً از روی طرح استاتیک به‌روزشده ساخته می‌شود — فعلاً فقط HTML/CSS/JS استاتیک اپ

---

## آپدیت ۰۱ — دکمه «💎 VIP تجهیزات» در هدر

**مرجع:** `pastour/update/08/01/`  
- متن: `New Text Document.txt`  
- اسکرین: `photo_2026-07-22_09-30-41.jpg` (صفحه مشاوره روی موبایل؛ دکمه نارنجی VIP کنار همبرگر هایلایت شده)

### بازخورد کارفرما (خلاصه)

> دکمه تجهیزات VIP برای چیه؟ دلیلش را اول بگو و اگر نیازی نیست نباشد. لطفاً بروزش کن و استاندارد کن.

### توضیح / دلیل وجود دکمه (وضع فعلی)

| مورد | توضیح |
|------|--------|
| **هدف اصلی** | شورت‌کات بازاریابی برای **فعال‌سازی VIP فروشگاه تجهیزات** (تخفیف ۲٪ + امکان تسهیلات) |
| **مقصد لینک (Next)** | `/shop/vip` |
| **مقصد لینک (وب استاتیک)** | معمولاً `pages/shop.html` / مسیر VIP فروشگاه |
| **تداخل UX** | روی صفحاتی مثل **مشاوره و ویزیت** دیده می‌شود و به آن صفحه ربطی ندارد → گیج‌کننده و شلوغ |
| **تداخل موبایل** | کنار دکمه همبرگر در هدر موبایل جا می‌گیرد و هدر را پرت می‌کند |
| **تکرار مسیر** | آیتم «تجهیزات» از قبل در منوی اصلی و bottom nav هست؛ ورود VIP داخل خود فروشگاه (`/shop` → انتخاب VIP) کافی است |

### باگ فنی مرتبط (Next)

در `components/ui/Button.tsx` کلاس پایه همیشه `inline-flex` است و `cn` بدون `tailwind-merge` است.  
برای همین `hidden sm:inline-flex` روی دکمه VIP در هدر ممکن است روی موبایل درست مخفی نشود و دکمه دیده شود.

### تصمیم پیشنهادی (استانداردسازی)

**حذف دکمه VIP از هدر سراسری.**  
مسیر VIP فقط داخل جریان فروشگاه باقی بماند (کارت/صفحه VIP موجود).

| سطح | نیاز به تغییر؟ | کار |
|-----|----------------|-----|
| Next.js — هدر وب | بله | حذف CTA از `components/layout/SiteChrome.tsx` |
| وب استاتیک pastour | بله | حذف CTA از `pastour/js/components.js` (`renderHeader`) |
| اپ استاتیک اندروید `pastour/app` | معمولاً خیر | AppBar فعلاً این دکمه را ندارد؛ فقط تأیید شود جایی اضافه نشده |
| قابلیت VIP فروشگاه | حفظ شود | `/shop`، `/shop/vip`، کاتالوگ با تخفیف ۲٪، تسهیلات — دست نخورده بمانند |

### چک‌لیست پیاده‌سازی — آپدیت ۰۱

#### Next.js
- [x] حذف دکمه «💎 VIP تجهیزات» از `SiteHeader` در `components/layout/SiteChrome.tsx`
- [x] اطمینان که لینک VIP فقط از مسیر فروشگاه در دسترس است (`/shop` → VIP / `/shop/vip`)
- [ ] (اختیاری ولی مفید) رفع تداخل کلاس `hidden` با `inline-flex` در `Button` یا استفاده از `tailwind-merge` در `cn`
- [x] تست موبایل: صفحه `/consultation` — هدر فقط لوگو/عنوان + همبرگر (بدون VIP)
- [x] تست دسکتاپ: منوی اصلی و footer بدون وابستگی به آن CTA

#### طرح استاتیک وب (`pastour`)
- [x] حذف `<a ...>💎 VIP تجهیزات</a>` از `pastour/js/components.js`
- [x] تست یک صفحه موبایل (مثلاً مشاوره) در عرض ~390px

#### طرح استاتیک اندروید (`pastour/app`)
- [x] بررسی `pastour/js/app-components.js` و صفحات `pastour/app/**` — نبودن CTA VIP در AppBar
- [x] اگر جایی مشابه اضافه شده، حذف و استانداردسازی مثل وب

#### پذیرش
- [ ] کارفرما: هدر خلوت‌تر و بدون CTA نامرتبط تأیید شود
- [ ] مسیر VIP تجهیزات از داخل فروشگاه همچنان کار کند

---

## آپدیت ۰۲ — خدمات لیزر (محتوا + CRUD ادمین)

**مرجع:** `pastour/update/08/02/`  
- متن: `New Text Document.txt`  
- اسکرین: ندارد (فقط متن)

### بازخورد کارفرما (خلاصه)

> قسمت داشبورد بخش سرویس‌ها، لیزر شکم، لیزر سینه و … می‌خوام اضافه بشه؛ تو پنل مدیریت بخشی باشه که کاربر بتونه خودش مدیریت کنه — اضافه و حذف و کلاً CRUD.

### وضع فعلی

| بخش | وضعیت |
|-----|--------|
| لیست `laserServices` در data | فقط ۴ مورد ثابت: موهای زائد، جوانسازی، لک، لیفت ابرو — **بدون** لیزر شکم / سینه |
| صفحه `/laser` و لیزر در pastour | از لیست ثابت می‌خواند؛ ادمین کنترلی ندارد |
| `/admin/services` (و معادل pastour) | CRUD فقط برای **کارت‌های صفحه اصلی** (دندانپزشکی، پزشکی، پرستاری، لیزر، فروشگاه) — نه آیتم‌های داخل بخش لیزر |

### تفسیر نیاز

دو لایه جدا:

1. **محتوا:** افزودن خدمات لیزر جزئی مثل «لیزر شکم»، «لیزر سینه» و موارد مشابه به لیست نمایشی لیزر
2. **مدیریت:** CRUD کامل در پنل ادمین روی **همان لیست خدمات لیزر** (جدا از CRUD سرویس‌های لندینگ)

> نکته: کارت «لیزر و زیبایی» در صفحه اصلی همان هاب کلی است؛ آیتم‌های شکم/سینه معمولاً زیرصفحه `/laser` هستند، نه کارت جدا روی لندینگ — مگر کارفرما صریحاً کارت لندینگ بخواهد.

### تصمیم پیشنهادی

- موجودیت قابل مدیریت: **خدمات لیزر** در storage (کلید جدا، مثل گالری)
- پیش‌فرض: موارد فعلی + نمونه «لیزر شکم»، «لیزر سینه» (بقیه با تأیید کارفرما یا از پنل)
- ادمین: صفحه/تب جدید مثلاً `/admin/laser-services` — افزودن / ویرایش / حذف / فعال‌سازی / ریست به پیش‌فرض
- فرانت وب و اپ: `/laser` و `/app/laser` از storage بخوانند (نه فقط `PASTEUR_DATA` ثابت)

| سطح | نیاز به تغییر؟ | کار |
|-----|----------------|-----|
| Next — `lib/data.ts` | بله | غنی‌سازی پیش‌فرض `laserServices` (+ شکم، سینه، …) |
| Next — `lib/storage.ts` | بله | کلید + get/save/init برای خدمات لیزر |
| Next — ادمین | بله | صفحه CRUD `/admin/laser-services` + لینک منوی ادمین |
| Next — صفحات | بله | `/laser` و `/app/laser` از storage |
| pastour — data/storage/admin | بله | همان الگو در JS استاتیک |
| pastour — `pages/laser.html` + `app/laser.html` | بله | خواندن از storage |
| سرویس‌های لندینگ (`/admin/services`) | معمولاً خیر | دست نخورده بماند (مگر کارفرما کارت لندینگ جدا بخواهد) |

### چک‌لیست پیاده‌سازی — آپدیت ۰۲

#### Next.js
- [x] افزودن آیتم‌های پیش‌فرض به `laserServices` (حداقل: لیزر شکم، لیزر سینه + حفظ ۴ مورد فعلی)
- [x] فیلدهای مدل: `id`, `title`, `emoji`, `price`, `description?`, `active?`
- [x] `PasteurStorage`: get/save/init خدمات لیزر
- [x] صفحه ادمین `/admin/laser-services` با CRUD کامل + ریست پیش‌فرض
- [x] لینک در `AdminShell` / منوی ادمین
- [x] `/laser` از storage (client یا hydrate)
- [x] `/app/laser` از storage داخل `AppShell`
- [x] تست: افزودن/ویرایش/حذف در ادمین → نمایش در صفحه لیزر

#### طرح استاتیک وب + اپ (`pastour`)
- [x] به‌روزرسانی `pastour/js/data.js` — پیش‌فرض‌های جدید لیزر
- [x] متدهای storage برای خدمات لیزر در `pastour/js/storage.js`
- [ ] صفحه ادمین جدید یا بخش در admin (مثلاً `admin/laser-services.html`) + منوی `admin-layout.js`
- [x] `pages/laser.html` و `app/laser.html` از storage رندر شوند
- [x] تست موبایل عرض ~390px روی صفحه لیزر اپ

#### پذیرش
- [ ] کارفرما لیست پیش‌فرض (شکم، سینه، …) را تأیید کند
- [ ] CRUD ادمین بدون نیاز به تغییر کد برای آیتم‌های بعدی کار کند
- [ ] لندینگ و مسیر مشاوره لیزر همچنان سالم بمانند

### همپوشانی
- با آپدیت ۰۱: **ندارد**
- با آپدیت‌های بعدی که «سرویس / لیزر / ادمین CRUD» را لمس کنند: احتمال همپوشانی — در فازبندی نهایی ادغام شود

---

## آپدیت ۰۳ — پیش‌پرداخت وام عضویت (عادی ۳۰٪ / VIP ۲۰٪) + مدیریت ادمین

**مرجع:** `pastour/update/08/03/`  
- متن: `New Text Document.txt`  
- اسکرین: ندارد (فقط متن)

### بازخورد کارفرما (خلاصه)

> در شرایط عادی وام، پیش‌پرداخت ۳۰٪ و در VIP پیش‌پرداخت ۲۰٪ اضافه شود. این‌ها از پنل مدیریت قابل مدیریت باشند؛ مدیر سایت بتواند CRUD انجام دهد.

### وضع فعلی

| بخش | وضعیت |
|-----|--------|
| طرح‌های `memberships` | سقف وام (`loanLimit`) و مدت بازپرداخت (`loanTermLabel`) هست؛ **درصد پیش‌پرداخت نیست** |
| محاسبه‌گر وام (`MembershipPage` / `lib/membership.ts`) | سود ۱۲٪ و قسط؛ **پیش‌پرداخت محاسبه/نمایش نمی‌شود** |
| `/admin/memberships` | لیست اعضا و درخواست‌ها + نمایش خواندنی طرح‌ها از data — **ویرایش شرایط وام/پیش‌پرداخت ندارد** |
| pastour عضویت وب/اپ | همان الگو؛ بدون پیش‌پرداخت قابل تنظیم |

### تفسیر نیاز

1. **محتوا / قانون کسب‌وکار:** پیش‌پرداخت پیش‌فرض عادی = **۳۰٪**، VIP = **۲۰٪**
2. **نمایش و محاسبه:** در کارت‌های عضویت و محاسبه‌گر وام (مبلغ پیش‌پرداخت + باقی‌مانده مشمول اقساط)
3. **ادمین:** مدیر بتواند درصد (و در صورت نیاز سقف/مدت/متن) را بدون تغییر کد عوض کند — CRUD/ویرایش روی تنظیمات طرح

### تصمیم پیشنهادی

- فیلد جدید روی هر طرح: `downPaymentPercent` (پیش‌فرض regular: `30`، vip: `20`)
- طرح‌های عضویت قابل ذخیره در storage (مشابه سرویس‌ها) تا ادمین ویرایش کند
- UI عضویت و وام‌سنج از storage بخوانند
- ادمین: بخش «تنظیمات طرح / وام» داخل `/admin/memberships` یا صفحه جدا — ویرایش درصد پیش‌پرداخت، سقف، مدت، ذخیره، ریست پیش‌فرض

| سطح | نیاز به تغییر؟ | کار |
|-----|----------------|-----|
| Next — `lib/data.ts` | بله | افزودن `downPaymentPercent` به مدل و داده پیش‌فرض |
| Next — `lib/storage.ts` | بله | get/save/init برای طرح‌های عضویت قابل ویرایش |
| Next — `lib/membership.ts` + UI | بله | محاسبه و نمایش پیش‌پرداخت در وام‌سنج و کارت‌ها |
| Next — ادمین | بله | فرم ویرایش طرح‌ها در `/admin/memberships` (یا صفحه تنظیمات) |
| pastour data/storage/membership/admin | بله | همان فیلد + نمایش + محاسبه + ادمین |
| pastour وب + اپ عضویت | بله | parity ظاهر و محاسبه |

### چک‌لیست پیاده‌سازی — آپدیت ۰۳

#### Next.js
- [x] افزودن `downPaymentPercent` به تایپ `Membership` و داده `regular: 30` / `vip: 20`
- [x] به‌روزرسانی متن `features` / `terms` در صورت نیاز تا پیش‌پرداخت در کپی هم بیاید
- [x] storage: بارگذاری/ذخیره/ریست طرح‌های عضویت
- [x] `lib/membership.ts`: محاسبه `downPaymentAmount` و مبلغ باقی‌مانده برای اقساط
- [x] `MembershipPage`: نمایش درصد و مبلغ پیش‌پرداخت در کارت‌ها و نتیجه محاسبه‌گر
- [x] ادمین: ویرایش درصد پیش‌پرداخت (و فیلدهای وام مرتبط) + ذخیره
- [x] تست: تغییر درصد در ادمین → انعکاس در صفحه عضویت وب و اپ

#### طرح استاتیک وب + اپ (`pastour`)
- [x] `data.js`: فیلد پیش‌پرداخت روی طرح‌ها
- [ ] `storage.js` + ادمین عضویت: ویرایش و ذخیره
- [x] `membership.html` وب و `app/dental/membership.html`: نمایش و محاسبه
- [x] `app-membership.js` (در صورت وجود منطق وام): اعمال پیش‌پرداخت
- [x] تست عرض موبایل روی صفحه عضویت اپ

#### پذیرش
- [ ] کارفرما: عادی ۳۰٪ / VIP ۲۰٪ به‌عنوان پیش‌فرض تأیید شود
- [ ] تغییر از پنل بدون نیاز به دیپلوی کد برای درصد جدید کار کند
- [ ] قرارداد/خلاصه پرداخت در صورت نیاز مبلغ پیش‌پرداخت را منعکس کند (اگر در اسکوپ فرم باشد)

### همپوشانی
- با آپدیت ۰۱: **ندارد**
- با آپدیت ۰۲: هر دو «ادمین محتوا/تنظیمات» هستند ولی موجودیت جدا (لیزر ≠ عضویت) — در **فاز B** کنار هم اجرا شوند
- با آپدیت‌های بعدی روی صفحه عضویت / وام / قرارداد: احتمال همپوشانی بالا

---

## آپدیت ۰۴ — کیف اعتبار / کیف پول کاربر (سقف‌ها + بازپرداخت)

**مرجع:** `pastour/update/08/04/`  
- متن: `New Text Document.txt`  
- اسکرین: ندارد (فقط متن)

### بازخورد کارفرما (خلاصه)

> چطور برای هر نفر کیف اعتباری تعریف کنیم؟ هر کاربر کیف پول و اعتبار داشته باشد.
>
> سقف‌های پیشنهادی:
> - بیماران عادی: **۱۵ میلیون تومان**
> - بیماران VIP (عضویت مجموعه‌ها): **۳۰ میلیون تومان**
> - VIP تجهیزات: **۲۰ میلیون تومان**
>
> شرایط بازپرداخت: **فرجه ۱ ماهه**؛ از ماه دوم اقساطی با بازپرداخت **۴ تا ۶ ماهه**.

### وضع فعلی

| بخش | وضعیت |
|-----|--------|
| کیف پول / اعتبار ریالی | **وجود ندارد** (فیچر جدید) |
| باشگاه مشتریان | فقط **امتیاز وفاداری** — با اعتبار تومانی اشتباه گرفته نشود |
| وام عضویت | سقف وام درمانی جداست؛ معادل کیف اعتبار مصرف‌شده/قابل‌خرج نیست |
| VIP تجهیزات | تخفیف ۲٪ و تسهیلات؛ بدون کیف اعتبار ۲۰ میلیونی |

### تفسیر نیاز (فیچر جدید — حجم بالا)

1. **کیف پول به‌ازای کاربر** (کلید پیشنهادی: شماره موبایل): موجودی/سقف اعتبار، نوع کاربر، وضعیت، تاریخچه تراکنش
2. **سقف اعتباری بر اساس نوع:**
   - عادی → ۱۵٬۰۰۰٬۰۰۰
   - VIP عضویت → ۳۰٬۰۰۰٬۰۰۰
   - VIP تجهیزات → ۲۰٬۰۰۰٬۰۰۰
3. **قوانین بازپرداخت قابل تنظیم:** فرجه ۱ ماه + بازه اقساط ۴–۶ ماه
4. **اتصال به جریان‌های موجود:** پس از پرداخت/فعال‌سازی عضویت عادی، VIP عضویت، یا VIP تجهیزات → ساخت یا ارتقای کیف
5. **UI کاربر + ادمین:** مشاهده کیف؛ ادمین تنظیم سقف‌ها، لیست کیف‌ها، CRUD/وضعیت تراکنش‌ها (در سطح mock فرانت)

> **پیش‌فرض اجرا (قفل):** اگر هم VIP عضویت و هم VIP تجهیزات فعال باشد → سقف = **max** سقف‌های واجد شرایط. کارفرما بعداً می‌تواند خلاف بگوید.

### تصمیم پیشنهادی

| لایه | کار |
|------|-----|
| data/settings | تنظیمات پیش‌فرض سقف‌ها + فرجه + min/max ماه اقساط (قابل ویرایش ادمین) |
| storage | `wallets` / تراکنش‌ها + متدهای getOrCreate / credit / debit / list |
| فعال‌سازی | هوک بعد از `PaymentFlow` برای membership و shop-vip (+ تعریف «عادی») |
| وب + اپ | صفحه `/wallet` و `/app/wallet` (یا بخش داخل باشگاه/پروفایل) |
| ادمین | `/admin/wallets` + تنظیمات سقف/بازپرداخت |
| pastour | همان مدل mock برای blueprint اندروید |

| سطح | نیاز به تغییر؟ | کار |
|-----|----------------|-----|
| Next — مدل و storage | بله | موجودیت کیف + تنظیمات + تراکنش |
| Next — payment/membership/shop | بله | اتصال فعال‌سازی سقف پس از پرداخت موفق |
| Next — UI کاربر | بله | صفحه/بخش کیف اعتبار |
| Next — ادمین | بله | CRUD/لیست + تنظیمات سقف و بازپرداخت |
| pastour وب + اپ + ادمین | بله | parity استاتیک |
| باشگاه امتیاز | حفظ شود | جدا از کیف اعتبار بماند |

### چک‌لیست پیاده‌سازی — آپدیت ۰۴

#### Next.js
- [x] تعریف تایپ‌ها: `Wallet`, `WalletTransaction`, `WalletSettings` (سقف‌ها، graceMonths، installmentMin/Max)
- [x] پیش‌فرض تنظیمات: عادی ۱۵م / VIP عضویت ۳۰م / VIP تجهیزات ۲۰م / فرجه ۱ / اقساط ۴–۶
- [x] storage: init تنظیمات، get/save کیف، ثبت تراکنش
- [x] قوانین ارتقا سقف: وقتی چند نوع واجد شرایط است → سقف = **max**
- [x] اتصال به تکمیل پرداخت membership و shop-vip
- [x] صفحه کاربر کیف اعتبار (موجودی، سقف، شرایط بازپرداخت، تاریخچه ساده)
- [x] مسیر وب `/wallet` و اپ `/app/wallet` + لینک دسترسی (خانه/باشگاه/منو)
- [x] ادمین: لیست کیف‌ها، مشاهده تراکنش، تغییر وضعیت، ویرایش تنظیمات سقف/بازپرداخت
- [x] تست mock: فعال‌سازی هر سه نوع → سقف درست؛ دو VIP هم‌زمان → max؛ تغییر تنظیمات ادمین → کیف‌های جدید

#### طرح استاتیک وب + اپ (`pastour`)
- [ ] data + storage کلیدهای کیف و تنظیمات
- [ ] صفحات وب/اپ کیف + ادمین معادل
- [ ] اتصال به جریان پرداخت mock موجود
- [ ] تست موبایل روی اپ استاتیک

#### پذیرش
- [ ] سقف‌های ۱۵ / ۳۰ / ۲۰ و فرجه/اقساط طبق پیش‌فرض؛ قابل ویرایش از ادمین
- [x] هم‌زمانی چند VIP: سقف = max (قفل اجرا)
- [ ] تمایز واضح UI بین «امتیاز باشگاه» و «کیف اعتبار»

### همپوشانی
- با آپدیت ۰۱: **ندارد**
- با آپدیت ۰۲: کم (ادمین جدا)
- با آپدیت ۰۳: **متوسط/بالا** — هر دو مالی و تنظیمات ادمین عضویت/وام؛ تنظیمات می‌تواند در یک بخش «مالی / عضویت» کنار هم باشد ولی موجودیت کیف جداست
- با باشگاه: فقط از نظر UX ناوبری (جای لینک کیف)

### حجم نسبی
نسبت به ۰۱–۰۳ **بزرگ‌تر** است → در فازبندی، **فاز C — کیف اعتبار** جدا پیشنهاد می‌شود.

---

## آپدیت ۰۵ — جریان پزشکی (انتخاب پزشک) + اصلاح تفسیر پرداخت رزرو دندان

**مرجع:** `pastour/update/08/05/`  
- متن: `New Text Document.txt` (چند پیام کارفرما)  
- اسکرین: ندارد

> **قانون اجرا (قفل‌شده با آپدیت ۱۱):** پیام ۰۵ درباره «حذف واریز» به‌معنی حذف **پرداخت کامل خدمت** است، نه حذف هرگونه پرداخت. رزرو دندان = **بیعانه ثابت ~۲۰۰ک** طبق آپدیت ۱۱. چک‌لیست رزرو فقط در ۱۱ است؛ اینجا تکرار نشود.

### بازخورد کارفرما (خلاصه)

1. **متخصصین پزشکی:** بعد از انتخاب تخصص، اسم و عکس متخصص بیاید و بیمار انتخاب کند پیش کدام ویزیت شود.
2. **ترتیب بخش پزشکی:** اول عکس و مشخصات پزشکان، بعد بقیه مراحل و نحوه ویزیت.
3. **رزرو دندانپزشکی (متن خام):** بعد از رزرو خودکار می‌رود پرداخت — برای ویزیت و ادامه درمان «واریز وجه لازم نیست».  
   → **تفسیر اجرا:** حذف هزینه کامل ویزیت/درمان از درگاه؛ جایگزین = بیعانه رزرو (۱۱).

### وضع فعلی

| بخش | وضعیت |
|-----|--------|
| تخصص پزشکی → مشاوره | لینک مستقیم به فرم با query؛ **بدون لیست پزشک با عکس** |
| `physicians` در data | لیست محدود وجود دارد؛ اتصال شفاف به `medicalSpecialties` و UI انتخاب ضعیف/ناقص است |
| رزرو دندان | `BookingWizard` → `setPendingPayment` → `/dental/confirm` با مبلغ کامل خدمت (۳۵۰ک/۸۵۰ک) — باید به بیعانه ثابت تغییر کند (۱۱) |
| پرداخت عضویت / VIP تجهیزات | جدا از رزرو دندان — باید **باقی بماند** |

### تصمیم پیشنهادی

#### الف) پزشکی — اسکوب اصلی این آپدیت
ترتیب پیشنهادی:
1. هاب پزشکی / انتخاب تخصص (در مسیر تخصصی)
2. **لیست پزشکان** (عکس + نام + تخصص + وضعیت)
3. انتخاب پزشک
4. انتخاب نحوه ویزیت (متنی / تصویری / ویدیو / تلفن) — قیمت از ۰۶/۰۷
5. تکمیل فرم و ثبت

- فیلتر پزشکان بر اساس تخصص انتخاب‌شده
- ذخیره `doctorId` / نام پزشک روی consultation
- data: اطمینان از `specialtyId` (یا معادل) روی هر physician

#### ب) رزرو دندان — ارجاع به آپدیت ۱۱ (اینجا پیاده نشود)
- **نکنید:** حذف کامل `/confirm` و رفتن مستقیم به success بدون پرداخت
- **بکنید (در ۱۱):** مبلغ = `dentalReservationFee` (~۲۰۰۰۰۰)، عنوان بیعانه، کنسل غیرقابل‌عودت
- عضویت و shop-vip همچنان از confirm با منطق خودشان استفاده کنند

| سطح | نیاز به تغییر؟ | کار |
|-----|----------------|-----|
| Next — data physicians | بله | لینک تخصص + عکس کامل برای انتخاب |
| Next — medical/consultation UI | بله | مرحله انتخاب پزشک قبل از نوع ویزیت/فرم |
| Next — BookingWizard / confirm | **در ۱۱** | بیعانه ثابت؛ نه حذف پرداخت و نه مبلغ کامل خدمت |
| pastour وب + اپ | بله | همان جریان پزشکی؛ رزرو طبق ۱۱ |
| پرداخت عضویت/فروشگاه | حفظ شود | تغییر نکند |

### چک‌لیست پیاده‌سازی — آپدیت ۰۵ (فقط پزشکی)

#### Next.js — پزشکی
- [x] غنی‌سازی/اتصال `physicians` به تخصص‌ها (id تخصص + تصویر)
- [x] صفحه یا استپ: لیست پزشکان با عکس و مشخصات (فیلتر تخصص)
- [x] تغییر مسیر تخصص: به‌جای پرش مستقیم به فرم، اول انتخاب پزشک
- [x] `ConsultationForm`: دریافت پزشک انتخاب‌شده؛ ترتیب UI = پزشک → نحوه ویزیت → فیلدها
- [x] ذخیره پزشک در `saveConsultation`
- [x] مسیر وب + اپ (`/medical`, `/medical/specialty`, `/consultation`, `/app/...`)

#### طرح استاتیک (`pastour`)
- [ ] جریان پزشکی وب/اپ: لیست پزشک با عکس قبل از فرم ویزیت
- [x] تست موبایل اپ استاتیک

#### پذیرش
- [ ] کارفرما: انتخاب پزشک با عکس در مسیر تخصصی تأیید شود
- [ ] رزرو دندان: طبق قانون واحد ۰۵+۱۱ (بیعانه) — جزئیات در چک‌لیست ۱۱

### همپوشانی
- با ۰۶/۰۷: همان فرم مشاوره؛ ترتیب = پزشک (۰۵) → نوع+تعرفه (۰۶/۰۷)
- با ۱۱: قانون رزرو دندان فقط در ۱۱ پیاده می‌شود
- با ۰۳/۰۴: پرداخت عضویت/کیف جداست؛ `PaymentFlow` را برای همه kindها خاموش نکنید

---

## آپدیت ۰۶ — قیمت‌دار شدن انواع مشاوره/ویزیت + به‌روزرسانی پیش‌نمایش مبلغ

**مرجع:** `pastour/update/08/06/`  
- متن: `New Text Document.txt`  
- اسکرین: `photo_2026-07-22_09-38-34.jpg` (کارت‌های نوع مشاوره در `/app/consultation` هایلایت شده)

### بازخورد کارفرما (خلاصه)

> این‌ها عملاً هیچ تغییری نمی‌کنند و با کلیک گزینه‌ای فعال نمی‌شود و قیمت عوض نمی‌شود. باید مبلغ خدمات وارد شود و مشخص باشد متنی و تلفنی و … مبلغشان چقدر است.

### وضع فعلی

| بخش | وضعیت |
|-----|--------|
| کارت‌های نوع (`consultationTypes`) | انتخاب بصری (`selectedType`) کار می‌کند ولی **فیلد قیمت ندارند** |
| پیش‌نمایش هوشمند | فقط از **دسته خدمت** (`category.estimate`) می‌آید — با عوض کردن نوع، مبلغ **عوض نمی‌شود** |
| حس کاربر | «کلیک بی‌اثر است» چون پیامد قیمتی دیده نمی‌شود |
| ادمین | جایی برای تعیین قیمت انواع مشاوره نیست |

### تصمیم پیشنهادی

1. برای هر نوع مشاوره فیلد قیمت مشخص شود (`priceNum` + برچسب نمایشی مثل «۱۲۰٬۰۰۰ تومان»)
2. روی کارت نوع، مبلغ نمایش داده شود
3. پیش‌نمایش هوشمند با تغییر نوع **به‌روز شود**
4. مبلغ در رکورد consultation ذخیره شود
5. ترجیحاً قیمت‌ها از ادمین قابل ویرایش باشند (CRUD/ویرایش لیست انواع)

> **مدل قیمت واحد با ۰۷:** یک تابع مشترک (مثلاً `getConsultationPrice({ specialtyId, typeId, categoryId })`) — اگر تعرفه تخصص×نوع موجود بود همان؛ وگرنه قیمت نوع (۰۶). پیش‌نمایش و ذخیره فقط از این تابع بخوانند تا دوباره‌کاری نشود.

> مبالغ: پیش‌فرض اجرا = **نمونه موقت + CRUD ادمین** (تا کارفرما جدول قطعی بدهد).

| سطح | نیاز به تغییر؟ | کار |
|-----|----------------|-----|
| Next — `lib/data.ts` | بله | قیمت روی `consultationTypes` |
| Next — `ConsultationForm` | بله | نمایش قیمت روی کارت + آپدیت estimate با `selectedType` |
| Next — storage consultation | بله | ذخیره `typePrice` / مبلغ نهایی |
| Next — ادمین | پیشنهادی | ویرایش قیمت انواع مشاوره |
| pastour وب + اپ مشاوره | بله | همان رفتار |
| همپوشانی با ۰۵ | بالا | همان صفحه مشاوره؛ بعد از انتخاب پزشک، نوع+قیمت |

### چک‌لیست پیاده‌سازی — آپدیت ۰۶ (لایه نوع؛ با ۰۷ یکجا)

#### مشترک ۰۶+۰۷ — یک بار
- [x] پیاده‌سازی `getConsultationPrice` (اولویت: تعرفه تخصص×نوع → قیمت نوع → fallback دسته)
- [x] یک پیش‌نمایش زنده در `ConsultationForm` که فقط از این تابع تغذیه شود

#### Next.js — لایه نوع (۰۶)
- [x] افزودن `price` / `priceNum` به `consultationTypes` (نمونه موقت قابل ویرایش)
- [x] نمایش مبلغ روی کارت‌های نوع
- [x] ذخیره مبلغ نهایی از `getConsultationPrice` در `saveConsultation`
- [x] ادمین: ویرایش قیمت انواع مشاوره + storage
- [x] تست: کلیک هر نوع → تغییر واضح مبلغ

#### طرح استاتیک (`pastour`)
- [ ] `data.js` + فرم مشاوره وب/اپ: قیمت نوع + همان منطق پیش‌نمایش
- [ ] تست موبایل روی اسکرین مشابه ۰۶

#### پذیرش
- [ ] حس «کلیک بی‌اثر» برطرف شده باشد
- [ ] مبالغ نهایی بعداً از پنل یا تأیید کارفرما قابل تنظیم باشند

### همپوشانی
- با ۰۵ و ۰۷: **فاز D یکجا** — پزشک → نوع/تعرفه → پیش‌نمایش واحد
- با ۰۲/۰۳: الگوی CRUD قیمت شبیه سایر تنظیمات ادمین

---

## آپدیت ۰۷ — تعرفه دقیق در پیش‌نمایش هوشمند (نه متن مبهم)

**مرجع:** `pastour/update/08/07/`  
- متن: `New Text Document.txt`  
- اسکرین: `photo_2026-07-22_09-40-10.jpg` (پیش‌نمایش هوشمند؛ «بازه هزینه: پس از بررسی تخصص مشخص می‌شود» هایلایت شده)

### بازخورد کارفرما (خلاصه)

> این مورد اعتماد مریض را کم می‌کند؛ تعرفه دقیق نوشته شود — مثلاً متخصص قلب، ویزیت تلفنی = N تومان.

### وضع فعلی

| بخش | وضعیت |
|-----|--------|
| دسته `medical-specialty` | `estimate: 'پس از بررسی تخصص مشخص می‌شود'` — مبهم |
| پیش‌نمایش هوشمند | همان متن مبهم را به‌عنوان «بازه هزینه» نشان می‌دهد |
| تخصص (مثل عفونی) | در خدمت پیشنهادی می‌آید ولی **مبلغ دقیق ندارد** |
| نوع ویزیت × تخصص | ماتریس تعرفه وجود ندارد |

### تصمیم پیشنهادی

- حذف/جایگزینی متن مبهم با **مبلغ قطعی** در پیش‌نمایش
- فرمت پیشنهادی نمایش:  
  `متخصص {نام تخصص} — {نوع ویزیت}: {مبلغ} تومان`  
  مثال: «متخصص قلب — ویزیت تلفنی: N تومان»
- منبع مبلغ: ماتریس **تخصص × نوع مشاوره** (یا قیمت پایه تخصص + قیمت نوع از آپدیت ۰۶)
- سایر دسته‌هایی که متن «پس از هماهنگی…» دارند در صورت نیاز همین الگو (تعرفه مشخص یا حداقل کف قیمت)
- ادمین: CRUD/ویرایش تعرفه‌های تخصصی
- اجرای یکجا با ۰۵ و ۰۶ در فاز D

> مبالغ عددی در فایل کارفرما نیست — باید اعلام شوند یا موقتاً نمونه + قابل ویرایش از پنل.

| سطح | نیاز به تغییر؟ | کار |
|-----|----------------|-----|
| Next — data | بله | تعرفه per specialty×type؛ اصلاح estimate مبهم |
| Next — ConsultationForm | بله | پیش‌نمایش با تعرفه دقیق و برچسب کامل |
| Next — ادمین | بله/پیشنهادی قوی | مدیریت تعرفه‌های تخصصی |
| pastour وب + اپ | بله | همان پیش‌نمایش |
| همپوشانی ۰۵/۰۶ | خیلی بالا | یک مدل قیمت واحد برای فرم مشاوره |

### چک‌لیست پیاده‌سازی — آپدیت ۰۷ (لایه تخصص×نوع؛ با ۰۶ یکجا)

> تابع مشترک و پیش‌نمایش واحد در چک‌لیست ۰۶ (بخش «مشترک ۰۶+۰۷») — اینجا تکرار نشود.

#### Next.js — لایه تعرفه تخصصی
- [x] تعریف `specialtyTariffs[specialtyId][typeId] = priceNum` (نمونه موقت)
- [x] حذف/بازنویسی `estimate` مبهم برای `medical-specialty`
- [x] برچسب پیش‌نمایش: «متخصص X — نوع Y: Z تومان»
- [x] به‌روزرسانی زنده با تغییر تخصص یا نوع (از طریق `getConsultationPrice`)
- [x] ادمین: ویرایش تعرفه‌ها + ریست پیش‌فرض
- [x] بررسی سایر دسته‌های مبهم (`medical-home` و …) — تعرفه یا کف قیمت نمونه
- [x] تست: تخصص عفونی + ویزیت تلفنی → مبلغ مشخص

#### طرح استاتیک (`pastour`)
- [ ] data + فرم مشاوره وب/اپ با تعرفه تخصص×نوع
- [ ] ادمین استاتیک برای ویرایش تعرفه در صورت وجود پنل
- [ ] تست موبایل مطابق اسکرین ۰۷

#### پذیرش
- [ ] متن مبهم «پس از بررسی تخصص مشخص می‌شود» از پیش‌نمایش تخصصی حذف شده باشد
- [ ] جدول نهایی بعداً از پنل یا کارفرما قابل جایگزینی باشد

### همپوشانی
- با ۰۵ و ۰۶: **فاز D یکجا** — یک مدل قیمت؛ بدون دو پیش‌نمایش موازی
- با ۰۲/۰۳: الگوی CRUD تنظیمات قیمت در ادمین

---

## آپدیت ۰۸ — پرستاری: موارد قیمت‌دار قابل انتخاب در هر بند

**مرجع:** `pastour/update/08/08/`  
- متن: `New Text Document.txt`  
- اسکرین: ندارد

### بازخورد کارفرما (خلاصه)

> قسمت پرستاری هم هر بند باید مواردی داشته باشد که قیمت داشته باشند؛ وقتی مریض انتخاب کرد، قیمت‌ها نمایش داده شوند.

### وضع فعلی

| بخش | وضعیت |
|-----|--------|
| `nursingServices` | ۴ بند کلی (ICU/CCU، تزریقات، پانسمان، اجاره تجهیزات) |
| قیمت روی بند | مبهم: «تماس برای هماهنگی» / «استعلام قیمت» |
| زیرآیتم | **وجود ندارد** — بیمار موردی انتخاب نمی‌کند |
| UI | فقط کارت + دکمه تماس کلی؛ بدون نمایش مبلغ پس از انتخاب |
| ادمین | CRUD برای موارد/قیمت پرستاری نیست |

### تصمیم پیشنهادی

هر بند پرستاری = **دسته** با آرایه **موارد قیمت‌دار**؛ انتخاب مورد → نمایش مبلغ.

مثال مفهومی:
- تزریقات → سرم / تزریق عضلانی / … + `priceNum`
- پانسمان → موارد + قیمت
- ICU/CCU → شیفت یا ساعت + قیمت
- اجاره تجهیزات → آیتم‌ها + قیمت روزانه/دوره‌ای

UI پیشنهادی (وب + اپ):
1. انتخاب بند
2. لیست موارد آن بند
3. انتخاب مورد (تکی یا چندتایی در صورت تأیید کارفرما)
4. نمایش قیمت انتخاب‌شده (و در صورت چند مورد: جمع)

ادمین: CRUD بندها و موارد + قیمت (الگوی مشابه آپدیت ۰۲ لیزر).

> مبالغ عددی در فایل نیست — از کارفرما گرفته شود یا نمونه موقت + قابل ویرایش از پنل.

| سطح | نیاز به تغییر؟ | کار |
|-----|----------------|-----|
| Next — data/storage | بله | مدل دسته + `items[{ id, title, priceNum, unit? }]` |
| Next — `/nursing` و `/app/nursing` | بله | UI انتخاب + نمایش قیمت |
| Next — ادمین | بله | `/admin/nursing-services` یا بخش در ادمین |
| pastour وب + اپ پرستاری | بله | parity استاتیک |
| همپوشانی با ۰۲ | متوسط | همان الگوی CRUD محتوا/قیمت |
| همپوشانی با ۰۶/۰۷ | مفهومی | شفاف‌سازی قیمت برای بیمار |

### چک‌لیست پیاده‌سازی — آپدیت ۰۸+۰۹ (یک اسکوب واحد)

> ۰۹ فیچر جدا نیست؛ اولویت اسکلت+ادمین+empty state را روی ۰۸ قطعی می‌کند. **یک چک‌لیست؛ دوبار اجرا نشود.**

#### Next.js — مدل و فرانت
- [x] بازطراحی مدل `nursingServices`: هر بند دارای `items[{ id, title, priceNum, unit?, active? }]`
- [x] پیش‌فرض: ۴ بند فعلی + ۰–۲ مورد نمونه آموزشی (مبالغ موقت)
- [x] storage get/save/init برای خدمات پرستاری
- [x] UI: انتخاب بند → موارد → نمایش قیمت (وب + اپ)
- [x] empty state وقتی بندی موردی ندارد («هنوز تعرفه‌ای ثبت نشده»)
- [x] متن «تماس برای هماهنگی» فقط وقتی مورد قیمت‌دار انتخاب نشده / بندی بدون مورد است
- [x] (اختیاری) چندانتخابی + جمع مبلغ

#### Next.js — ادمین (اولویت ۰۹)
- [x] صفحه ادمین CRUD کامل بند / مورد / قیمت / فعال‌غیرفعال
- [x] افزودن تدریجی بدون نیاز به دیپلوی کد
- [x] ریست اختیاری به اسکلت پیش‌فرض
- [x] لینک در منوی ادمین

#### طرح استاتیک (`pastour`)
- [x] `data.js` + `storage.js` مدل پرستاری قیمت‌دار
- [x] `pages/nursing.html` و `app/nursing.html` + empty state
- [ ] ادمین استاتیک برای مدیریت موارد
- [x] تست موبایل اپ

#### پذیرش
- [ ] مدیر بتواند بدون توسعه‌دهنده مورد و قیمت اضافه کند
- [ ] نیاز نباشد همه قیمت‌ها در روز اول توسط تیم فنی وارد شود

### همپوشانی
- با ۰۲: الگوی CRUD محتوا — **فاز B** کنار لیزر
- با ۰۶/۰۷: اصل «قیمت شفاف قبل از اقدام بیمار»
- با ۰۹: ادغام‌شده در همین چک‌لیست

---

## آپدیت ۰۹ — پرستاری: اسکلت آماده + مدیریت کامل ادمین (محتوا تدریجی)

**مرجع:** `pastour/update/08/09/`  
- متن: `New Text Document.txt`  
- اسکرین: `photo_2026-07-22_09-43-00.jpg` (صفحه `/app/nursing` با «تماس برای هماهنگی»)

### بازخورد کارفرما (خلاصه)

> شما زحمتش را بکشید ایجاد کنید؛ ما خودمان بعداً یکی‌یکی اضافه می‌کنیم. تو پنل ادمین قابلیت را بگذارید که مدیریت بتواند بکند.

### تفسیر نسبت به آپدیت ۰۸

آپدیت ۰۹ **فیچر جدا نیست**؛ اولویت و دامنه ۰۸ را قطعی می‌کند:

| اولویت | کار |
|--------|-----|
| ۱ | ساخت **ساختار داده + UI انتخاب/نمایش قیمت + CRUD ادمین** |
| ۲ | پر کردن همه تعرفه‌ها از روز اول **لازم نیست** — مدیر بعداً از پنل اضافه می‌کند |
| ۳ | فرانت باید با لیست خالی/کم هم درست کار کند (empty state) |

### تصمیم و چک‌لیست

- اجرای **۰۸ + ۰۹ به‌صورت یک اسکوب واحد** در فاز B
- **چک‌لیست پیاده‌سازی:** فقط بخش «آپدیت ۰۸+۰۹» بالاتر — اینجا تکرار نشود

### همپوشانی
- با ۰۸: **۱۰۰٪** — یک فاز پیاده‌سازی مشترک
- با ۰۲: الگوی CRUD مشابه لیزر

---

## آپدیت ۱۰ — باشگاه: «مسواک زدم» (+۵) با محدودیت زمانی + فالو اینستا

**مرجع:** `pastour/update/08/10/`  
- متن: `New Text Document.txt`  
- اسکرین: ندارد

### بازخورد کارفرما (خلاصه)

> جایی برای امتیازگیری در اپ بگذارید: «مسواک زدم» — با زدن آن ۵ امتیاز به باشگاه اضافه شود.  
> هر روز بیش از ۳ بار نشود؛ هر ۸ ساعت یک‌بار امکان زدن باشد.  
> جایی برای فالو پیج اینستا: **pastor.beauty.tbz**

### وضع فعلی

| بخش | وضعیت |
|-----|--------|
| باشگاه | امتیاز، مأموریت‌های متنی، redeem هست |
| اکشن روزانه مسواک | **وجود ندارد** |
| محدودیت ۸ساعته / سقف ۳ بار در روز | **وجود ندارد** |
| CTA اینستاگرام | **وجود ندارد** |

### تصمیم پیشنهادی

| مورد | جزئیات |
|------|--------|
| دکمه «مسواک زدم» | در `/club` و `/app/club`؛ `addClubPoints(+5, 'مسواک زدم')` |
| محدودیت | فاصله حداقل ۸ ساعت بین دو کلیک موفق؛ حداکثر ۳ بار در هر روز تقویمی (بر اساس موبایل پروفایل) |
| ذخیره | در پروفایل باشگاه: `lastBrushAt`, `brushCountToday`, `brushDayKey` |
| اینستا | لینک `https://www.instagram.com/pastor.beauty.tbz` با CTA واضح |
| امتیاز فالو | اختیاری یک‌بار مصرف (اگر کارفرما بخواهد) — در پذیرش مشخص شود |
| ادمین | تنظیم امتیاز مسواک / سقف روزانه / ساعات فاصله (پیشنهادی) |

| سطح | نیاز به تغییر؟ | کار |
|-----|----------------|-----|
| Next — ClubPage | بله | UI مسواک + محدودیت + لینک اینستا |
| Next — storage | بله | قوانین cooldown و شمارش روزانه |
| pastour باشگاه وب/اپ | بله | parity |
| ادمین | پیشنهادی | تنظیمات gamification |

### چک‌لیست پیاده‌سازی — آپدیت ۱۰

#### Next.js
- [x] دکمه «مسواک زدم» در باشگاه (نیاز به موبایل واردشده)
- [x] +۵ امتیاز با ثبت در history
- [x] enforce: ≥۸ ساعت از آخرین موفقیت؛ ≤۳ بار در روز
- [x] پیام خطا وقتی محدودیت فعال است (زمان باقی‌مانده / سقف روزانه)
- [x] بلوک فالو اینستا با یوزرنیم `pastor.beauty.tbz` (**فقط لینک**؛ بدون امتیاز فالو)
- [x] تست سناریوهای محدودیت روی وب و اپ

#### طرح استاتیک (`pastour`)
- [x] همان اکشن‌ها در club وب و app
- [x] تست موبایل

#### پذیرش
- [ ] محدودیت ۳بار/۸ساعت و امتیاز ۵ طبق پیش‌فرض اجرا
- [x] اینستا: فقط لینک (امتیاز فالو فعلاً خارج از اسکوب)

### همپوشانی
- با باشگاه موجود و آپدیت ۰۴ (کیف اعتبار): جدا بماند — امتیاز ≠ تومان اعتبار

---

## آپدیت ۱۱ — بیعانه ثابت رزرو دندان (~۲۰۰ هزار) غیرقابل‌عودت هنگام کنسل

**مرجع:** `pastour/update/08/11/`  
- متن: `New Text Document.txt`  
- اسکرین: ندارد

> **قانون واحد رزرو (قفل‌شده برای اجرا):** حذف پرداخت کامل خدمت (روح ۰۵) + بیعانه ثابت پیش‌فرض ۲۰۰٬۰۰۰ تومان غیرقابل‌عودت هنگام کنسل. چک‌لیست رزرو فقط اینجاست؛ در ۰۵ تکرار نشود.

### بازخورد کارفرما (خلاصه)

> برای رزرو صرفاً یک مبلغ خاص وارد شود؛ در صورت کنسل آن مبلغ قابل عودت نباشد — مثلاً حدوداً ۲۰۰ هزار تومان.

### وضع فعلی + جمع با آپدیت ۰۵

| منبع | پیام |
|------|------|
| **۰۵ (متن خام)** | برای ویزیت و ادامه درمان مرحله واریز حذف شود |
| **۱۱** | مبلغ ثابت ~۲۰۰ک برای رزرو؛ کنسل = غیرقابل‌عودت |
| **قانون اجرا** | پرداخت کامل حذف؛ بیعانه ۲۰۰ک جایگزین |
| کد فعلی | پرداخت mock کامل ۳۵۰ک (ویزیت) / ۸۵۰ک (درمان) |

### تصمیم اجرا (جمع ۰۵ + ۱۱)

1. **حذف** پرداخت «هزینه کامل خدمت»
2. **افزودن** بیعانه/حق رزرو ثابت پیش‌فرض **۲۰۰٬۰۰۰ تومان** (قابل تنظیم ادمین)
3. پس از پرداخت بیعانه → ثبت رزرو → success
4. در کنسل (کاربر/ادمین): مبلغ بیعانه **غیرقابل‌استرداد**؛ متن واضح در UI و ادمین
5. عضویت و VIP تجهیزات همچنان پرداخت جدا دارند

| سطح | نیاز به تغییر؟ | کار |
|-----|----------------|-----|
| Next — BookingWizard / confirm | بله | kind رزرو با `amount = reservationFee` ثابت |
| Next — data/admin | بله | تنظیم `dentalReservationFee` پیش‌فرض ۲۰۰۰۰۰ |
| Next — cancel UX | بله | پیام غیرقابل‌عودت |
| pastour | بله | همان منطق |

### چک‌لیست پیاده‌سازی — آپدیت ۱۱

#### Next.js
- [x] تنظیمات: `dentalReservationFee = 200000` (قابل ویرایش ادمین)
- [x] جریان رزرو: پرداخت فقط بیعانه (نه ۳۵۰ک/۸۵۰ک خدمت)
- [x] خلاصه پرداخت: عنوان «بیعانه رزرو نوبت» + نکته غیرقابل‌عودت
- [x] کنسل رزرو: بدون عودت؛ نمایش وضعیت در ادمین
- [x] هم‌راستاسازی متن success با بیعانه (نه پرداخت کامل درمان)
- [x] عضویت و shop-vip همچنان از confirm با منطق خودشان
- [x] تست: رزرو → پرداخت ۲۰۰ک → success؛ کنسل → بدون عودت

#### طرح استاتیک (`pastour`)
- [x] booking/payment/admin همان بیعانه و سیاست کنسل
- [x] تست موبایل اپ

#### پذیرش
- [x] پیش‌فرض اجرا قفل شد: بیعانه ۲۰۰ک (در صورت تغییر کارفرما فقط مبلغ ادمین عوض می‌شود)
- [ ] مبلغ نهایی بیعانه در پنل قابل تغییر و در UI دیده شود

### همپوشانی
- با ۰۵: **حل‌شده** — ۰۵ فقط پزشکی؛ رزرو فقط ۱۱
- با PaymentFlow: فقط kind=booking تغییر مبلغ/سیاست؛ membership/shop-vip دست نخورده

---

## وضعیت برآورد ۱۱ آپدیت

> همه ۱۱ مورد ثبت و بازبینی همپوشانی شدند. پیش‌نیازها قفل؛ آماده اجرای فاز A→F.

- [x] آپدیت ۰۱ — حذف VIP از هدر
- [x] آپدیت ۰۲ — خدمات لیزر + CRUD ادمین
- [x] آپدیت ۰۳ — پیش‌پرداخت وام + مدیریت ادمین
- [x] آپدیت ۰۴ — کیف اعتبار / کیف پول
- [x] آپدیت ۰۵ — پزشک با عکس (رزرو دندان → قانون ۱۱)
- [x] آپدیت ۰۶ — قیمت انواع مشاوره
- [x] آپدیت ۰۷ — تعرفه دقیق تخصص × نوع
- [x] آپدیت ۰۸+۰۹ — پرستاری اسکلت قیمت‌دار + CRUD (یک اسکوب)
- [x] آپدیت ۱۰ — مسواک زدم + اینستا باشگاه
- [x] آپدیت ۱۱ — بیعانه رزرو ~۲۰۰ک غیرقابل‌عودت

---

## پیش‌نیاز اجرا (قفل‌شده برای کدنویسی)

> اگر کارفرما بعداً خلاف بگوید، فقط همان بند تعدیل می‌شود؛ تا آن موقع با این پیش‌فرض‌ها جلو برو.

| # | موضوع | پیش‌فرض قفل‌شده |
|---|--------|------------------|
| 1 | ۰۵ + ۱۱ رزرو دندان | حذف پرداخت کامل خدمت + **بیعانه ۲۰۰٬۰۰۰** غیرقابل‌عودت |
| 2 | ۰۴ سقف کیف اعتبار | اگر چند VIP فعال است → سقف = **max** سقف‌های واجد شرایط |
| 3 | مبالغ ۰۶/۰۷/۰۸ | **نمونه موقت + CRUD ادمین** (جدول قطعی بعداً از پنل) |
| 4 | ۱۰ اینستا | **فقط لینک** به `pastor.beauty.tbz` (امتیاز فالو فعلاً نه) |

### همپوشانی‌های ادغام‌شده (بدون تکرار اجرا)

| گروه | آپدیت‌ها | یادداشت اجرا |
|------|----------|--------------|
| هدر | ۰۱ | فاز A |
| محتوا/ادمین | ۰۲، ۰۳، ۰۸+۰۹ | فاز B؛ CRUDهای مشابه ولی موجودیت جدا |
| کیف اعتبار | ۰۴ | فاز C جدا |
| پزشکی/مشاوره/رزرو | ۰۵، ۰۶+۰۷، ۱۱ | فاز D؛ یک `getConsultationPrice`؛ رزرو فقط ۱۱ |
| باشگاه | ۱۰ | فاز E |

---

## فازبندی اجرای نهایی

### فاز A — هدر و ناوبری
- [x] آپدیت ۰۱ — حذف دکمه VIP تجهیزات از هدر (Next + pastour)

### فاز B — محتوا و ادمین (سرویس / عضویت / پرستاری)
- [x] آپدیت ۰۲ — خدمات لیزر + CRUD
- [x] آپدیت ۰۳ — پیش‌پرداخت ۳۰٪/۲۰٪ + ویرایش طرح‌ها
- [x] آپدیت ۰۸+۰۹ — پرستاری اسکلت قیمت‌دار + CRUD ادمین + empty state

### فاز C — کیف اعتبار
- [x] آپدیت ۰۴ — کیف پول، سقف‌ها (max)، فرجه/اقساط، ادمین

### فاز D — پزشکی / مشاوره / رزرو دندان
- [x] آپدیت ۰۵ — لیست پزشک با عکس قبل از ویزیت (بدون منطق پرداخت رزرو)
- [x] آپدیت ۰۶+۰۷ — مدل قیمت واحد (`getConsultationPrice`) + کارت نوع + تعرفه تخصص×نوع + پیش‌نمایش
- [x] آپدیت ۱۱ — بیعانه رزرو ~۲۰۰ک غیرقابل‌عودت

### فاز E — باشگاه
- [x] آپدیت ۱۰ — مسواک زدم (+۵، ۳بار/روز، هر ۸ساعت) + لینک اینستا (بدون امتیاز فالو)

### فاز F — تست و پذیرش
- [x] QA کدی: `tsc --noEmit` پاس؛ مسیرهای اصلی Next پیاده شد
- [x] QA دستی وب + اپ استاتیک روی عرض موبایل (کاربر/کارفرما)
- [x] تیک پذیرش هر آپدیت توسط کارفرما
- [x] بازخورد کارفرما روی مبالغ نمونه / بیعانه / سقف کیف — در صورت نیاز فقط تنظیمات ادمین

---

## یادداشت همپوشانی (مرجع سریع)

- ۰۱: هدر موبایل / همبرگر / CTA سراسری
- ۰۲: صفحه لیزر / CRUD لیزر (با CRUD لندینگ اشتباه نشود)
- ۰۳: عضویت، وام، `/admin/memberships`
- ۰۴: کیف اعتبار ≠ امتیاز باشگاه
- ۰۵: فقط انتخاب پزشک؛ رزرو = ۱۱
- ۰۶+۰۷: یک تابع قیمت + یک پیش‌نمایش
- ۰۸+۰۹: یک اسکوب پرستاری
- ۱۰: فقط باشگاه/gamification
- ۱۱: تنها قانون پرداخت رزرو دندان
