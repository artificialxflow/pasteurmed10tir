# TODO v4 — اصلاح فرانشیز٪، تأیید کاربری، اقساط صحیح، ادمین کامل

> هدف: جمع‌آوری بازخورد `updates/02/`، برآورد، و پیاده‌سازی فازبندی‌شده روی:
>
> 1. **پروژه Next.js فعلی** (فرانت اصلی — منبع حقیقت)
> 2. **طرح استاتیک اندروید** داخل `pastour/app/` (parity پس از پایدار شدن Next)
>
> مرجع:
> - `updates/02/01/New Text Document.txt` — ادمین کامل + فرانشیز٪ + تأیید کاربری + شکایات ادمین
> - `updates/02/02/New Text Document.txt` + `updates/02/02/photo_2026-07-29_16-56-01.jpg` — مشاوره/ویزیت با پیش‌نمایش ۳۵۰هزار؛ بعد از تأیید بیمه تکمیلی فقط ٪ فرانشیز واریز شود
> - `updates/02/03/New Text Document.txt` — (۱) مبنای پورسانت معرف؟ (۲) VIP پرداختی + اعتبار ۳۰م + وام ۲۰۰م به ادمین نرسید
> - `updates/02/04/New Text Document.txt` + `updates/02/04/photo_2026-07-29_16-58-08.jpg` — سررسید شمسی؛ اسکرین «اقساط من» اشتباهاً حق عضویت (VIP ۱٫۶م / عادی ۱م) را ۶ قسط کرده؛ باید وام تأییدشده باشد
> - `updates/02/05/New Text Document.txt` — **دو مورد اصلی باقی‌مانده از نظر کارفرما:** (۱) اقساط اعتبار ۱۵/۳۰م جدا از تسهیلات؛ نه قسط‌بندی حق عضویت (۲) فرانشیز٪ روی هزینه ویزیت (مثال ۳۵۰هزار × ۲۰٪ = ۷۰هزار)؛ بقیه را کارفرما حل‌شده دانسته
>
> نکته شماره‌گذاری: «آپدیت ۰۵/۰۶/۰۷» داخل این todo = اپیک‌های داخلی‌اند و با پوشهٔ `updates/02/05` یکی نیستند. پوشه ۰۲/۰۵ عمدتاً به اپیک فرانشیز٪ (۰۲) و اقساط (۰۷) برمی‌گردد.
>
> وضعیت: **اجرای v4 شروع شد** — اولویت B→C→D و E.
>
> ارتباط: ادامهٔ `todo-v3.md` (پروفایل/بیمه/اقساط/ادمین پایه). این سند **اصلاح مدل و شکاف‌های پذیرش** است، نه جایگزینی کامل v3.

---

## اصول مشترک

- [ ] هر تغییر کاربرمحور در **وب** و **`/app`** اعمال شود
- [ ] تا قبل از بک‌اند: منطق **mock / localStorage**؛ هوک برای API بعدی بماند
- [ ] بدون بازطراحی آزاد؛ فقط طبق بازخورد کارفرما
- [ ] قبل از فیچر جدید: **ممیزی تطابق ادمین** در فاز A (اولویت پایین‌تر از B–E مگر بلاکر پذیرش)
- [ ] pastour استاتیک: parity بعد از قفل شدن Next در همان فاز

---

## خلاصه بازخورد کارفرما

| # | موضوع | منبع |
|---|--------|------|
| ۱ | پنل ادمین باید با همه قابلیت‌های سامانه تطابق داشته باشد؛ کامل چک شود | ۰۲/۰۱ |
| ۲ | فرانشیز **درصد** است نه تومان | ۰۲/۰۱، ۰۲/۰۵ |
| ۳ | پنل کاربری بیمار «در حال بررسی»؛ بعد از تأیید ادمین، درصد فرانشیز روی ویزیت اعمال شود — ادمین تأیید کاربری ندارد | ۰۲/۰۱ |
| ۴ | شکایات در ادمین پیدا نشد (کشف‌پذیری / منو / نقش) | ۰۲/۰۱ |
| ۵ | پس از ثبت بیمه تکمیلی + تأیید ما: مبلغ واریزی = `(فرانشیز٪ × هزینه ویزیت)`؛ مثال‌ها: ۳۵۰هزار × ۱۰٪ = ۳۵هزار (۰۲/۰۲) یا × ۲۰٪ = ۷۰هزار (۰۲/۰۵) | ۰۲/۰۲، ۰۲/۰۵ |
| ۶ | مبنای پورسانت معرف‌ها چیست؟ حق عضویت یا چه؟ — الان روی `amount` همان پرداخت (مثلاً بیعانه/حق عضویت پرداخت‌شده) است؛ باید شفاف و قفل شود | ۰۲/۰۳ |
| ۷ | سناریو: پرداخت VIP → اعتبار ۳۰م باز شد → درخواست وام ۲۰۰م — از ثبت‌نام VIP چیزی به ادمین نیامد (کشف‌پذیری یا ثبت ناقص در memberships/wallets/facilities) | ۰۲/۰۳ |
| ۸ | سررسید اقساط شمسی باشد (اسکرین: `2026-08-29` میلادی — اشتباه) | ۰۲/۰۴ متن+اسکرین |
| ۹ | اقساط‌بندی روی حق عضویت اشتباه است؛ مبنا = وام/تسهیلات **تأییدشده** (۰۲/۰۴) و/یا بسته اعتباری جدا (۰۲/۰۵) | ۰۲/۰۴، ۰۲/۰۵ |
| ۱۰ | شاهد باگ: کارت‌های «عضویت» VIP ۱٬۶۰۰٬۰۰۰ و عادی ۱٬۰۰۰٬۰۰۰ هر کدام ۶ قسط با برچسب عضویت | ۰۲/۰۴ اسکرین |
| ۱۱ | اولویت کارفرما (۰۲/۰۵): فرانشیز٪ + اقساط درست اصلی‌اند → اجرای **B→C→D** و **E**؛ F برای وام/اقساط facility؛ A/G در صورت نیاز | ۰۲/۰۵ |

---

## ممیزی کد در برابر ۰۲/۰۵ (وضع اجرا — Jul 2026)

> هدف این بخش: جدا کردن «زیرساخت v3 موجود» از «کار v4 مانده». هیچ فاز v4 تیک اجرا نخورده.

### مورد اصلی ۱ — اقساط اعتبار ≠ تسهیلات ≠ حق عضویت

| جزء | وضعیت | شواهد کد |
|-----|--------|----------|
| صفحه «اقساط من» وب/اپ | انجام‌شده (v3) | `InstallmentsPage`، مسیرهای `/installments` |
| باز شدن کیف/اعتبار بعد از عضویت | نسبتاً انجام‌شده | `upgradeWalletForUser` در `lib/payment.ts` |
| قسط‌بندی اشتباه حق عضویت به ۶ قسط | **باگ فعال — مانده** | `createMembershipInstallmentPlan` هنوز از پرداخت عضویت صدا زده می‌شود؛ `source: 'membership'` |
| اقساط از روی بسته اعتباری ۱۵/۳۰م | **مانده** | سازندهٔ `credit` جدا نیست؛ فقط برچسب UI «کیف اعتبار» برای `wallet` بدون ساخت صحیح |
| اقساط جدا بعد از تأیید تسهیلات/وام | **مانده** | `/admin/facilities` فقط وضعیت را عوض می‌کند؛ `saveInstallmentPlan` با source تسهیلات ندارد |
| دو منبع هم‌زمان = دو کارت + دو سررسید | **مانده** | مدل: `'membership' \| 'wallet'`؛ نیاز به `credit` / `facility` |
| سررسید شمسی | **مانده** | `nextDue` خام ISO در UI |
| empty-state متن «پس از پرداخت عضویت نمونه ساخته می‌شود» | باید عوض شود | `InstallmentsPage` |

→ فاز اجرایی: **E** (+ تأیید وام از **F**)

### مورد اصلی ۲ — فرانشیز درصدی روی هزینه ویزیت

| جزء | وضعیت | شواهد کد |
|-----|--------|----------|
| فیلد فرانشیز در پروفایل بیمار | انجام جزئی — **واحد اشتباه** | `franchiseAmount` + لیبل «فرانشیز (تومان)» در `AccountPage` |
| استعلام بیمه mock + تأیید نمایشی | انجام‌شده (v3) | `ConfirmPayment` / ادمین insurances |
| مبلغ واریزی = ٪ × هزینه ویزیت | **مانده** | بعد از approve، `amount = franchiseAmount` تومان ثابت (پیش‌فرض ۵۰٬۰۰۰) |
| مثال QA: ۳۵۰هزار × ۲۰٪ = ۷۰هزار (یا ۱۰٪ = ۳۵هزار) | **مانده** | `payableFromFranchise` وجود ندارد؛ `franchisePercent` نیست |
| نمایش مبلغ٪ در مشاوره (پیش‌نمایش ۳۵۰هزار) | **مانده** | تیک آنلاین+تکمیلی هست؛ مبلغ واریزی٪ نیست |
| تأیید کاربری بیمار قبل از اعمال٪ | **مانده** (از ۰۲/۰۱؛ برای گیت اعمال٪ لازم) | بدون `status` روی پروفایل / بدون `/admin/patients` |

→ فاز اجرایی: **B → C → D**

### موارد جانبی ۰۲ (طبق حرف کارفرما در ۰۲/۰۵ «بقیه حل شده» — اولویت پایین‌تر مگر خلافش ثابت شود)

| موضوع | در todo | اولویت پیشنهادی |
|--------|---------|-----------------|
| ممیزی ادمین + شکایات | A / ۰۱ / ۰۴ | بعد از B–E یا موازی کوتاه اگر بلاکر پذیرش است |
| VIP→ادمین | F / ۰۶ | لازم برای تکمیل اقساط facility؛ حداقل مسیر تأیید وام |
| مبنای پورسانت | G / ۰۵ | شفاف‌سازی؛ فرمول تا قفل کارفرما عوض نشود |

---

## وضع فعلی پروژه (فاصله با نیاز)

| حوزه | الان | فاصله |
|------|------|--------|
| فرانشیز | `franchiseAmount` به تومان در پروفایل و پرداخت | باید `franchisePercent`؛ واحد UI «درصد» |
| تأیید پروفایل بیمار | ثبت فوری؛ بدون وضعیت / بدون ادمین | وضعیت `pending/approved/...` + صفحه ادمین |
| مشاوره/ویزیت (`ConsultationForm`) | پیش‌نمایش هوشمند قیمت کامل (مثلاً ۳۵۰هزار)؛ تیک آنلاین+تکمیلی؛ **ثبت درخواست بدون درگاه فرانشیز٪** | بعد از تأیید بیمه/کاربری: نمایش و در صورت پرداخت، مبلغ = ٪ × همان قیمت ویزیت |
| محاسبه پرداخت بیمه (دندان/`ConfirmPayment`) | مبلغ ≈ فرانشیز ثابت تومان جای بیعانه | `visitFee × percent / 100`؛ مبنا = هزینه ویزیت نه بیعانهٔ ثابت |
| شکایات ادمین | مسیر `/admin/complaints` و آیتم منو وجود دارد | کشف‌پذیری برای نقش‌های محدود / QA با کارفرما |
| اقساط | بعد از پرداخت عضویت: `createMembershipInstallmentPlan` → کارت‌هایی مثل اسکرین ۰۲/۰۴ (برچسب عضویت، کل=حق عضویت، ۶ قسط، سررسید ISO) | توقف کامل؛ منبع = اعتبار و/یا تسهیلات تأییدشده؛ بدون کارت عضویت؛ سررسید شمسی |
| سررسید | نمایش خام `YYYY-MM-DD` میلادی (مثال اسکرین: `2026-08-29`) | نمایش با تقویم شمسی (`fa-IR`) برای بیمار و ادمین |
| عضویت VIP → ادمین | پرداخت → `saveMember` + `upgradeWallet`؛ فرم ثبت‌نام ممکن است `saveMembershipApplication`؛ وام جدا در `facilityRequests` / صفحات shop یا membership | کارفرما هر سه را از «ثبت‌نام VIP» انتظار دارد؛ باید در ادمین واضح و مرتبط دیده شوند |
| پورسانت | `saveCommission`: `% ویزیتور × amount` تراکنش (`booking`=`pending.amount` اغلب بیعانه؛ `membership`/`shop-vip`=مبلغ پرداخت عضویت) | سؤال کارفرما بی‌پاسخ است؛ UI مبنا + قفل قانون |

---

## پیش‌نیاز اجرا (قفل موقت — تا تأیید کارفرما)

| # | موضوع | پیش‌فرض اجرا |
|---|--------|----------------|
| 1 | مبنای فرانشیز٪ | درصد ۰–۱۰۰ از **همان مبلغ ویزیت نمایش‌داده‌شده**. معیارهای QA: ۳۵۰٬۰۰۰ × ۱۰٪ = ۳۵٬۰۰۰ (۰۲/۰۲) و ۳۵۰٬۰۰۰ × ۲۰٪ = ۷۰٬۰۰۰ (۰۲/۰۵ — «بیمه ایران بیست درصد») |
| 2 | شرط اعمال | بیمه تکمیلی ثبت + **تأیید کارشناس/ما** → فقط همان ٪ به‌عنوان مبلغ واریزی (نه کل ویزیت) |
| 3 | بیعانه رزرو دندان (v2/v3) | بیعانه (~۲۰۰ک) با هزینه ویزیت قاطی نشود؛ مسیر بیمه+٪ جدا محاسبه شود |
| 4 | اقساط عضویت | **ساخته نشود**؛ هوک `createMembershipInstallmentPlan` حذف/غیرفعال |
| 5 | منابع اقساط | `credit` = بازپرداخت بسته اعتباری ۱۵/۳۰م (مهلت/تا ۶ ماه طبق قواعد محصول)؛ `facility` = وام تأییدشده؛ هم‌زمان = دو کارت + دو سررسید + دو یادآور |
| 6 | مبنای پورسانت | تا قفل کارفرما: رفتار فعلی + برچسب UI؛ اولویت پایین‌تر از فرانشیز٪ و اقساط (۰۲/۰۵) |
| 7 | تاریخ سررسید | همیشه شمسی در UI؛ ضدباگ `2026-08-29` خام |
| 8 | ترتیب اجرای پیشنهادی طبق ۰۲/۰۵ | **B → C → D** (فرانشیز٪) موازی با شروع **E** (توقف باگ عضویت)؛ سپس تکمیل E با تأیید وام از **F**؛ A/G در صورت نیاز پذیرش |

---

## آپدیت ۰۱ — ممیزی و تکمیل پنل ادمین

**مرجع:** `updates/02/01` خطوط ۱–۳ + شکایات

### بازخورد
> قبل از هر چیزی پنل ادمین را کامل چک کن… هر چیزی لازم است در پنل ادمین هم باشد.  
> برای دیدن شکایات هم در پنل ادمین چیزی نیست.

### تصمیم پیشنهادی
- ماتریس قابلیت بیمار/عمومی ↔ صفحه ادمین ↔ permission
- رفع کشف‌پذیری شکایات (برچسب منو، گروه سایدبار، نقش‌های demo)
- شکاف‌های یافت‌شده → تسک‌های فازهای بعدی (نه اسکوپ باز)

### چک‌لیست
- [ ] جدول ممیزی: رزرو، مشاوره، عضویت/VIP، فروشگاه، کیف/اعتبار، تسهیلات، باشگاه، بیمه/استعلام، نظرات، شکایات، آموزش، اقساط، پورسانت، یادآور، دسترسی
- [ ] برای هر ردیف: مسیر ادمین، permission در `lib/adminAccess.ts`، دیده شدن در `AdminShell` برای نقش `admin` و نقش‌های محدود
- [ ] QA دستی لاگین `admin` / `ops` / `content` / `finance` — آیا «شکایات» دیده می‌شود؟
- [ ] اگر آیتم هست ولی پیدا نمی‌شود: بهبود برچسب/گروه‌بندی منو یا لینک از داشبورد
- [ ] لیست شکاف‌های واقعی (مثلاً تأیید بیمار، اقساط وام) → ارجاع به فاز B/C/E
- [ ] مستند کوتاه نتیجه ممیزی داخل همین فایل یا بخش «نتیجه فاز A»

### همپوشانی
- ترتیب اجرا رسمی: **B→C→D** و **E** اول (طبق ۰۲/۰۵)؛ فاز A موازی/بعداً مگر بلاکر منو

---

## آپدیت ۰۲ — فرانشیز درصدی + اعمال روی هزینه ویزیت

**مرجع:**
- متن: `updates/02/01`، `updates/02/02`، `updates/02/05`
- اسکرین: `updates/02/02/photo_2026-07-29_16-56-01.jpg` (صفحه مشاوره و ویزیت — پیش‌نمایش هوشمند ۳۵۰٬۰۰۰ تومان + تیک آنلاین با بیمه تکمیلی)

### بازخورد
> وقتی طرف بیمه تکمیلی‌اش را زد و به تأیید ما رسید، درصد فرانشیز هرچه هست باید همان درصد از هزینه ویزیت پرداخت شود.  
> مثلاً ۳۵۰ هزار است و فرانشیز ۱۰٪ → مبلغ واریزی ۳۵ هزار نشان داده شود.  
> (تأکید ۰۲/۰۵: واحد فرانشیز درصد است نه تومان.)

### سناریوی اسکرین ۰۲/۰۲ (معیار پذیرش اصلی این آپدیت)
1. بیمار در پنل کاربری بیمه تکمیلی + فرانشیز٪ را ثبت می‌کند → وضعیت بررسی
2. ادمین کاربری را تأیید می‌کند (فاز C)
3. در مشاوره، نوع ویزیت طوری است که پیش‌نمایش **۳۵۰٬۰۰۰** نشان می‌دهد (مثل ویزیت تصویری پزشکی عمومی در داده)
4. تیک «ویزیت آنلاین با پوشش بیمه تکمیلی» فقط وقتی بیمه تکمیلی ثبت/واجد شرایط است فعال است
5. پس از تأیید: مبلغ قابل واریز / نمایش برای پرداخت = **۳۵٬۰۰۰** (۱۰٪ از ۳۵۰هزار) — نه ۳۵۰هزار کامل و نه یک مبلغ ثابت تومان به‌نام فرانشیز

### تصمیم پیشنهادی
- جایگزینی/مهاجرت فیلد: `franchiseAmount` → `franchisePercent` (۰–۱۰۰)
- تابع مشترک: `payableFromFranchise(visitFee, percent)` → `Math.round(visitFee * percent / 100)`
- `visitFee` = همان عدد قیمت ویزیت در UI (مشاوره: `priceNum` / پیش‌نمایش هوشمند؛ دندان: هزینه ویزیت مرتبط — جدا از بیعانهٔ رزرو مگر کارفرما یکی کند)
- لیبل UI همه‌جا: «فرانشیز (درصد)»
- دو سطح تأیید ممکن است لازم باشد: تأیید پروفایل (فاز C) و/یا استعلام بیمه؛ تا قفل پذیرش، هر دو را در چک‌لیست فاز D پوشش بده

### چک‌لیست — مدل و storage (فاز B)
- [ ] تایپ `PatientProfile` / `InsuranceInquiry` در `lib/patient.ts`: `franchisePercent`؛ فیلد تومان حذف یا فقط برای سازگاری موقت
- [ ] مهاجرت localStorage قدیمی (`franchiseAmount` → درصد پیش‌فرض یا ریست)
- [ ] پیش‌فرض نمونه ۱۰ + اعتبارسنجی ۰–۱۰۰
- [ ] ادمین بیمه‌ها/استعلام: ستون/نمایش درصد نه تومان
- [ ] util مشترک قابل استفاده از مشاوره و ConfirmPayment

### چک‌لیست — پروفایل بیمار (فاز B)
- [ ] `AccountPage` وب + اپ: ورودی «فرانشیز (درصد)»
- [ ] متن راهنما: پس از تأیید، فقط همین درصد از هزینه ویزیت پرداخت می‌شود

### چک‌لیست — مشاوره / ویزیت مطابق اسکرین ۰۲/۰۲ (فاز D — اولویت کارفرما)
- [ ] مشخص کردن منبع `visitFee` در `ConsultationForm` (همان مبلغ پیش‌نمایش هوشمند)
- [ ] وقتی شرایط بیمه+تأیید برقرار است: در کارت پیش‌نمایش یا زیر آن، **مبلغ واریزی فرانشیز٪** نشان داده شود (مثال: ۳۵۰هزار → ۳۵هزار)
- [ ] اگر هنوز تأیید نشده: پیام «در حال بررسی» و عدم اعمال٪ روی مبلغ واریزی
- [ ] تصمیم محصول (در پذیرش قفل): آیا مشاوره بعد از تأیید به **درگاه پرداخت** با مبلغ٪ می‌رود، یا فعلاً فقط نمایش مبلغ + ثبت درخواست کافی است؟ — پیش‌فرض پیشنهادی: حداقل نمایش صحیح مبلغ واریزی؛ درگاه اگر در فلو فعلی نیست اضافه شود فقط در حد لازم mock
- [ ] ذخیره روی consultation: `visitFee`, `franchisePercent`, `payableAmount` (در صورت اعمال)

### چک‌لیست — رزرو دندان / ConfirmPayment (فاز D)
- [ ] بعد از تأیید استعلام: `amount` درگاه = `payableFromFranchise(visitFee, percent)` نه `franchiseAmount` تومان
- [ ] UI خلاصه: هزینه ویزیت / درصد / مبلغ واریزی شفاف باشد؛ بیعانه با فرانشیز٪ قاطی نشود مگر قفل کارفرما

### چک‌لیست — pastour
- [ ] parity فیلد٪، پیش‌نمایش مبلغ واریزی در مشاوره، confirm دندان

### پذیرش
- [ ] QA: با فرانشیز ۱۰٪ و ویزیت ۳۵۰هزار → **۳۵هزار** (۰۲/۰۲)
- [ ] QA: با فرانشیز ۲۰٪ و ویزیت ۳۵۰هزار → **۷۰هزار** (۰۲/۰۵)
- [ ] واحد فیلد پروفایل دیگر «تومان» نباشد
- [ ] قفل باقی‌مانده: آیا تأیید پروفایل به‌تنهایی کافی است یا استعلام جدا هم لازم است؟

### وضعیت اجرا (ممیزی)
- [x] زیرساخت استعلام/تیک بیمه تکمیلی (v3)
- [ ] `franchisePercent` + util محاسبه
- [ ] UI٪ و مبلغ واریزی صحیح در مشاوره و ConfirmPayment
- [ ] گیت تأیید کاربری (فاز C)

---

## آپدیت ۰۳ — تأیید کاربری بیمار در ادمین

**مرجع:** ۰۲/۰۱ خطوط ۸–۱۱ (پیش‌نیاز اعمال٪ در آپدیت ۰۲)

### بازخورد
> پنل کاربری «در حال بررسی»؛ اگر تأیید شد درصد فرانشیز اعمال شود. در ادمین جایی برای تأیید کاربری نیست.

### تصمیم پیشنهادی
- وضعیت پروفایل: `pending` | `approved` | `rejected` (یا معادل فارسی در UI)
- ثبت/ویرایش بیمه و فرانشیز٪ → وضعیت `pending` تا تأیید ادمین
- اعمال فرانشیز٪ در پرداخت **فقط اگر** پروفایل `approved` (و در صورت نیاز استعلام بیمه هم `approved`)
- صفحه ادمین: لیست بیماران / صف تأیید

### چک‌لیست
- [ ] فیلد `status` (+ `reviewedAt`, `reviewNote` اختیاری) روی `PatientProfile`
- [ ] UI پنل کاربری: نمایش وضعیت («در حال بررسی» / «تأیید شده» / «رد شده»)
- [ ] مسیر ادمین مثلاً `/admin/patients` + permission `patients`
- [ ] اکشن تأیید / رد؛ بعد از تأیید امکان اعمال٪ در فلو پرداخت
- [ ] جلوگیری از دور زدن: اگر pending، مبلغ درگاه بیعانه/کامل بماند یا CTA استعلام غیرفعال با پیام واضح
- [ ] منوی `AdminShell` + نقش‌های دسترسی
- [ ] وب + اپ + (در صورت نیاز) pastour

### همپوشانی
- وابسته به آپدیت ۰۲ (درصد)
- با استعلام بیمه v3: هر دو تأیید لازم است یا یکی؟ → در پذیرش قفل شود

---

## آپدیت ۰۴ — کشف‌پذیری شکایات ادمین (+ QA)

**مرجع:** ۰۲/۰۱ خط ۱۲–۱۳

### بازخورد
> برای دیدن شکایات در پنل ادمین چیزی نیست؛ گشتم پیدا نکردم.

### وضع فعلی
- کد: `/admin/complaints` و آیتم منو با permission `complaints` وجود دارد

### چک‌لیست
- [ ] تأیید بصری منو برای کاربر `admin` کامل
- [ ] اگر نقش تست کارفرما محدود است: افزودن `complaints` به نقش مربوط یا توضیح در راهنما
- [ ] لینک میان‌بر از داشبورد ادمین (تعداد شکایات جدید)
- [ ] در صورت باگ فیلتر مسیر: رفع
- [ ] QA با کارفرما: «از لاگین تا لیست شکایات در کمتر از ۳ کلیک»

---

## آپدیت ۰۵ — مبنای پورسانت معرف‌ها

**مرجع:** `updates/02/03/New Text Document.txt` (پیام ۱۰:۵۷)

### بازخورد
> پورسانت معرف‌ها که یک درصدی می‌گذاریم؛ مبنا بر چه اساسی است؟ حق عضویت یا چی؟؟

### وضع فعلی کد (پاسخ فنی موقت — هنوز قفل کارفرما نیست)
- محاسبه در `PasteurStorage.saveCommission`: `commissionAmount = round(amount × visitor.commissionRate / 100)`
- caller در `lib/payment.ts` مقدار `amount` را از پرداخت می‌دهد:
  - `booking` → معمولاً همان مبلغ درگاه رزرو (بیعانه یا مبلغ پس از بیمه) — **نه لزوماً کل هزینه ویزیت**
  - `membership` → مبلغ حق عضویت پرداخت‌شده
  - `shop-vip` → مبلغ VIP تجهیزات پرداخت‌شده
- نرخ٪ روی کارت ویزیتور در `/admin/visitors` است؛ گزارش در `/admin/commissions`

### تصمیم پیشنهادی
1. **فاز شفاف‌سازی (اجباری قبل از تغییر فرمول):** در UI ادمین برای هر ردیف پورسانت نشان بده: منبع، برچسب مبنا، مبلغ پایه، نرخ٪، مبلغ پورسانت
2. **پرسش قفل از کارفرما** با گزینه‌های واضح (مثلاً فقط حق عضویت / بیعانه رزرو / کل ویزیت / حسب منبع متفاوت)
3. پس از پاسخ: اگر فرمول عوض شد، فقط از نقاط `payment.ts` (+ هر caller دیگر) مبلغ پایهٔ درست پاس شود؛ موتور `saveCommission` همان بماند مگر قانون پیچیده‌تر شود

### چک‌لیست — مستند و UI (فاز G)
- [ ] جدول کوتاه «منبع → الان مبنا چیست» در همین todo یا کامنت/راهنمای ادمین
- [ ] `/admin/commissions`: ستون‌های مبنا / مبلغ پایه / نرخ / پورسانت / `sourceType`
- [ ] `/admin/visitors`: کنار نرخ٪ یک جمله راهنما («پورسانت از مبلغ تراکنش معرف‌شده محاسبه می‌شود» تا قفل نهایی)
- [ ] در صورت وجود UI ورود کد معرف سمت بیمار: tooltip کوتاه که پورسانت برای معرف ثبت می‌شود (اختیاری)

### چک‌لیست — پس از قفل کارفرما
- [ ] اگر مبنا = حق عضویت فقط: رزرو یا منابع دیگر پورسانت نگیرند / یا پیام جدا
- [ ] اگر مبنا = کل ویزیت: در booking به‌جای `pending.amount` بیعانه، `visitFee` پاس شود
- [ ] اگر مبنا حسب منبع متفاوت است: نقشه منبع→مبلغ در یک تابع واحد (مثلاً `commissionBaseAmount(pending)`)
- [ ] تست با کد معرف نمونه روی رزرو و عضویت VIP

### پذیرش
- [ ] کارفرما یک قانون مبنا اعلام کند
- [ ] ادمین بدون ابهام ببیند پورسانت از چه عددی آمده

### همپوشانی
- با فاز F (پرداخت VIP): اگر پورسانت VIP هم بررسی شود، همان مبلغ حق عضویت فعلی است مگر قانون عوض شود
- با فاز D (فرانشیز٪): اگر رزرو با مبلغ٪ پرداخت شود، پورسانت فعلی از همان مبلغ کوچک‌تر حساب می‌شود — در قفل کارفرما مطرح شود

---

## آپدیت ۰۶ — VIP / اعتبار / وام باید به ادمین برسند

**مرجع:** `updates/02/03/New Text Document.txt` (پیام ۱۱:۱۲)

### بازخورد
> حق عضویت VIP را پرداخت کردم؛ همان طرح اعتباری ۳۰ میلیون باز شد و درخواست وام ۲۰۰ میلیونی زدم — از بخش ثبت‌نام VIP چیزی به پنل ادمین نیامد.

### سناریوی کارفرما (معیار پذیرش)
1. پرداخت حق عضویت VIP موفق
2. بسته / طرح اعتباری حدود ۳۰ میلیون برایش باز می‌شود
3. درخواست وام / تسهیلات ۲۰۰ میلیون ثبت می‌کند
4. انتظار: هر سه (یا حداقل پیامدهای ادمینی‌شان) در پنل ادمین دیده شوند — الان از نظر او «چیزی نیامد»

### وضع فعلی کد (فرضیهٔ فاصله)
| مرحله | احتمالاً کجا ذخیره می‌شود | صفحه ادمین |
|--------|---------------------------|------------|
| ثبت‌نام/فرم VIP قبل از پرداخت | `saveMembershipApplication` (از `MembershipPage`) | `/admin/memberships` (لیست درخواست‌ها) |
| پرداخت موفق VIP | `saveMember` + `upgradeWalletForUser` | اعضا در memberships؛ کیف در `/admin/wallets` |
| درخواست وام/تسهیلات | `saveFacilityRequest` (مثلاً `ShopFacility` و فلوهای مشابه) | `/admin/facilities` |

فاصلهٔ محتمل: داده در storage هست ولی **مسیر کشف از «ثبت‌نام VIP» یکپارچه نیست**؛ یا پرداخت بدون application؛ یا وام از مسیر دیگری می‌رود و در facilities نمی‌نشیند؛ یا نقش/منوی ادمین این صفحات را نشان نمی‌دهد.

### تصمیم پیشنهادی
- یک **صف / نمای یکپارچهٔ VIP** در ادمین (یا بخش‌های مرتبط با لینک متقابل): عضویت پرداخت‌شده ↔ اعتبار بازشده ↔ درخواست وام pending
- اطمینان از persist هر سه رویداد در سناریوی کارفرما
- شمارنده‌های داشبورد: درخواست عضویت جدید / تسهیلات جدید / کیف تازه ارتقا یافته
- این فاز با **فاز E (اقساط)** هم‌مرز است: وام تأییدشده باید بعداً طرح اقساط `facility` بسازد — اینجا فقط **رسیدن به ادمین + تأیید** است

### چک‌لیست — ردیابی فلو (فاز F)
- [ ] نقشهٔ UI: ثبت‌نام VIP دندان / shop-vip / وام از کجا submit می‌شود (وب + اپ)
- [ ] برای هر استپ لاگ/بررسی storage: application، member، wallet kinds/سقف ۳۰م، facility request ۲۰۰م
- [ ] اگر پرداخت VIP بدون `saveMembershipApplication` است → در completePayment هم application/رویداد ادمین ثبت شود یا عضو تازه در لیست «نیاز به توجه» بیاید
- [ ] اگر درخواست ۲۰۰م به `facilityRequests` نمی‌رسد → باگ ثبت رفع شود
- [ ] اگر می‌رسد ولی UI ادمین خالی/فیلتر اشتباه → رفع نمایش

### چک‌لیست — UI ادمین
- [ ] `/admin/memberships`: VIPهای تازه پرداخت‌شده واضح (تاریخ، طرح، موبایل، مبلغ)
- [ ] `/admin/wallets`: نمایش ارتقای اعتبار / سقف مرتبط با VIP (برچسب ۳۰م در صورت وجود در داده)
- [ ] `/admin/facilities`: درخواست وام ۲۰۰م با وضعیت pending + اکشن تأیید/رد
- [ ] داشبورد ادمین: لینک/badge به موارد جدید VIP/تسهیلات
- [ ] (اختیاری ولی مفید) صفحه یا تب «پیگیری VIP» که سه ستون را کنار هم برای یک موبایل نشان دهد
- [ ] QA نقش‌ها: کاربر `admin` هر سه منو را ببیند؛ اگر کارفرما با نقش محدود تست کرده، permission درست شود

### چک‌لیست — تست پذیرش
- [ ] سناریوی کامل mock از صفر: پرداخت VIP → دیدن عضو + کیف در ادمین → ثبت وام ۲۰۰م → دیدن در facilities
- [ ] pastour parity اگر همان فلو را دارد

### همپوشانی
- فاز A (ممیزی منو): کشف‌پذیری memberships/facilities/wallets
- فاز E: بعد از تأیید وام در facilities → ساخت اقساط جدا از حق عضویت
- فاز G: پورسانت روی همان پرداخت VIP اگر کد معرف داشته باشد

---

## آپدیت ۰۷ — اقساط: منبع درست + جداسازی اعتبار و تسهیلات + شمسی

**مرجع:**
- متن: `updates/02/04/New Text Document.txt`، تکمیل در `updates/02/05/New Text Document.txt`
- اسکرین باگ: `updates/02/04/photo_2026-07-29_16-58-08.jpg` (صفحه «اقساط من»)

### بازخورد ۰۲/۰۴ (متن)
> سررسیدها می‌شود بر اساس تاریخ شمسی باشد؟  
> اصلاً این اقساط‌بندی رفته روی حق عضویت.  
> اقساط‌بندی بر اساس وامی که درخواست شده و **تأیید شده** باید باشد.

### شاهد اسکرین ۰۲/۰۴ (باید بعد از اصلاح دیده نشود)
صفحه `/installments` (و معادل اپ) الان دو کارت با برچسب **عضویت** نشان می‌دهد:

| کارت | عنوان تقریبی | کل | پرداخت‌شده | مانده | اقساط | سررسید بعدی |
|------|--------------|-----|-------------|--------|--------|-------------|
| ۱ | اقساط عضویت یک‌ساله … VIP | ۱٬۶۰۰٬۰۰۰ | ۲۶۶٬۶۶۷ | ۱٬۳۳۳٬۳۳۳ | ۶ | `2026-08-29` |
| ۲ | اقساط عادی — عضویت ۱ ساله | ۱٬۰۰۰٬۰۰۰ | ۱۶۶٬۶۶۷ | ۸۳۳٬۳۳۳ | ۶ | `2026-08-29` |

یعنی: `paid ≈ total/6` بعد از پرداخت یکجای حق عضویت + سررسید میلادی — دقیقاً خلاف خواست کارفرما.

### تکمیل ۰۲/۰۵ (منبع درست چیست)
- بعد از پرداخت عضویت، **بسته اعتباری** ۱۵م/۳۰م برای بیمار باز می‌شود؛ اقساط‌بندی مربوط به **همان اعتبار** است (طبق قواعد مهلت/تعداد قسط محصول)، نه خودِ ۱م/۱٫۶م حق عضویت.
- **تسهیلات/وام** درخواست‌شده و تأییدشده اقساط **جدا** با سررسید **جدا** دارد.
- کسی که هم اعتبار هم تسهیلات دارد → دو کارت جدا در «اقساط من».

### تصمیم پیشنهادی
1. **توقف فوری** فراخوانی `createMembershipInstallmentPlan` از مسیر پرداخت عضویت (`lib/payment.ts` و pastour معادل)
2. منبع `membership` دیگر به‌صورت خودکار ساخته نشود؛ کارت‌های موجود با `source=membership` در UI مخفی یا پاک‌سازی mock
3. منابع مجاز:
   - `credit` — بازپرداخت بسته اعتباری (۱۵م/۳۰م)
   - `facility` / `loan` — بعد از **تأیید ادمین** روی درخواست تسهیلات (وابسته به فاز F)
4. برچسب کارت: «اعتبار» / «تسهیلات» — نه «عضویت»
5. سررسید در UI همیشه شمسی؛ معیار ضدباگ: دیگر `2026-08-29` خام به کاربر نشان داده نشود
6. یادآور سررسید per طرح جدا

### چک‌لیست — مدل (فاز E)
- [ ] `InstallmentPlan.source`: حداقل `credit` | `facility`؛ deprecate ساخت خودکار `membership`
- [ ] فیلدها: `linkedRequestId` / `linkedWalletId`، `approvedByAdmin`، `dueDates[]`، عنوان و برچسب منبع
- [ ] مهاجرت: طرح‌های فعلی با عنوان/منبع عضویت از لیست بیمار حذف یا flag `hidden`

### چک‌لیست — منطق (فاز E)
- [ ] حذف/غیرفعال هوک در `lib/payment.ts` بعد از `saveMember` (و pastour `payment.js`)
- [ ] ساخت طرح `credit` هنگام باز شدن/قوانین بازپرداخت بسته اعتباری (نه هنگام صرفاً واریز حق عضویت به‌عنوان اصل قسط)
- [ ] ساخت طرح `facility` فقط وقتی ادمین در `/admin/facilities` درخواست را تأیید کرد (مبلغ = مبلغ وام تأییدشده، مثلاً ۲۰۰م در سناریوی ۰۲/۰۳)
- [ ] یادآور `type: installment` جدا برای هر طرح با dueDate همان طرح
- [ ] اطمینان: پرداخت ۱٬۰۰۰٬۰۰۰ یا ۱٬۶۰۰٬۰۰۰ عضویت دیگر ردیف اقساط با همان مبلغ کل نسازد

### چک‌لیست — UI بیمار (معیار اسکرین ۰۲/۰۴)
- [ ] `InstallmentsPage` وب + `/app/installments`: بعد از پرداخت فقط‌عضویت، **لیست خالی یا فقط اعتبار/تسهیلات** — نه دو کارت عضویت اسکرین
- [ ] برچسب منبع غیر از «عضویت» برای طرح‌های معتبر
- [ ] نمایش سررسید با `toLocaleDateString('fa-IR')` یا util شمسی یکسان (نه ISO خام)
- [ ] اگر دو منبع فعال‌اند: دو کارت جدا، دو سررسید جدا
- [ ] pastour `/app/installments.html` همان رفتار

### چک‌لیست — ادمین
- [ ] `/admin/installments`: فیلتر منبع؛ عدم نمایش یا برچسب «منسوخ» برای membershipهای قدیمی
- [ ] از صفحه facilities بعد از تأیید → لینک به طرح اقساط ساخته‌شده

### پذیرش (ضد اسکرین باگ)
- [ ] بازتولید سناریوی کارفرما: واریز حق عضویت VIP و عادی → «اقساط من» دیگر کارت ۱٫۶م/۱م شش‌قسمتی نشان ندهد
- [ ] سررسید شمسی خوانا باشد
- [ ] پس از باز شدن اعتبار ۱۵/۳۰م: کارت اقساط **اعتبار** (نه عضویت) طبق قواعد مهلت/تعداد قسط
- [ ] پس از تأیید یک وام نمونه: کارت **تسهیلات** با مبلغ وام و سررسید جدا
- [ ] هم‌زمانی اعتبار + تسهیلات = دو کارت مجزا

### وضعیت اجرا (ممیزی ۰۲/۰۵)
- [x] صفحه اقساط من + یادآور mock (v3)
- [x] ارتقای کیف بعد از عضویت (نسبی)
- [ ] توقف قسط‌بندی حق عضویت (باگ فعال)
- [ ] سازنده اقساط `credit` و `facility`
- [ ] سررسید شمسی + متن empty-state صحیح
- [ ] pastour parity

### همپوشانی
- فاز F: بدون دیده شدن/تأیید وام در ادمین، اقساط `facility` کامل نمی‌شود
- فاز A: منوی اقساط ادمین در ممیزی باشد
- ۰۲/۰۵ همچنین فرانشیز٪ را تکرار می‌کند → همان فاز B/D؛ اینجا فقط اقساط
- کارفرما در ۰۲/۰۵: این + فرانشیز٪ دو مورد اصلی‌اند؛ بقیه را حل‌شده دانسته

---

## همپوشانی و ترتیب منطقی

```
اولویت ۰۲/۰۵ ★
B فرانشیز٪ مدل ──► C تأیید کاربری ──► D اعمال٪ روی ویزیت (۱۰٪/۲۰٪ از ۳۵۰هزار)
E توقف باگ عضویت + اقساط credit ──► (F تأیید وام) ──► اقساط facility
A / G فقط اگر بلاکر پذیرش یا وقت اضافه
H پذیرش با تمرکز روی دو مورد اصلی ۰۲/۰۵
```

---

## فازبندی اجرا

> **اولویت پذیرش ۰۲/۰۵:** ابتدا مسیر فرانشیز٪ (**B→C→D**) و اصلاح اقساط (**E**؛ حداقل توقف باگ عضویت). مسیر F برای تکمیل اقساط وام لازم است. A و G اولویت پایین‌تر مگر بلاکر QA.

### فاز A — ممیزی ادمین + شکایات
- [ ] آپدیت ۰۱ — ماتریس تطابق + رفع شکاف‌های کشف‌پذیری
- [ ] آپدیت ۰۴ — QA شکایات ادمین
- [ ] در ماتریس A صریحاً ردیف‌های memberships / wallets / facilities / commissions / installments را تیک بزن

### فاز B — مدل فرانشیز درصدی ⭐ اولویت ۰۲/۰۵
- [x] `franchisePercent` در تایپ/storage/پروفایل/ادمین بیمه (جایگزین تومان)
- [x] util `payableFromFranchise(visitFee, percent)`
- [x] مهاجرت/حذف `franchiseAmount` از UI

### فاز C — تأیید کاربری (گیت اعمال٪)
- [x] آپدیت ۰۳ — وضعیت پروفایل + `/admin/patients` + گیت «تا تأیید نشده ٪ اعمال نشود»

### فاز D — اعمال٪ روی هزینه ویزیت ⭐ اولویت ۰۲/۰۵
- [x] مشاوره: قیمت پیش‌نمایش + نمایش مبلغ واریزی٪ پس از تأیید
- [x] در صورت نیاز: استپ/درگاه mock با مبلغ٪ برای مشاوره — نمایش مبلغ٪ در پیش‌نمایش (ثبت درخواست همان فلو)
- [x] دندان/`ConfirmPayment`: مبلغ = ٪ × هزینه ویزیت (نه تومان ثابت)
- [x] QA منطق: ۱۰٪→۳۵هزار و ۲۰٪→۷۰هزار از مبنا ۳۵۰هزار (`payableFromFranchise`)
- [ ] pastour parity کامل فرانشیز٪ (توقف اقساط عضویت انجام شد)

### فاز E — اقساط اصلاحی ⭐ اولویت ۰۲/۰۵ (+ اسکرین ۰۲/۰۴)
- [x] توقف فوری `createMembershipInstallmentPlan` روی پرداخت عضویت (Next + pastour)
- [x] پاکسازی/مخفی کارت‌های membership (۱٫۶م VIP / ۱م عادی)
- [x] ساخت اقساط `credit` برای بسته ۱۵/۳۰م (مهلت/تا ۶ ماه)
- [x] ساخت اقساط `facility` فقط بعد از تأیید وام (هوک در facilities)
- [x] UI: برچسب ≠ عضویت؛ دو منبع = دو کارت + دو سررسید شمسی
- [x] اصلاح empty-state (دیگر وعده «نمونه بعد از عضویت» ندهد)
- [ ] QA دستی ضد اسکرین ۰۲/۰۴ + pastour کامل

### فاز F — VIP / اعتبار / وام در ادمین (۰۲/۰۳؛ لازم برای E کامل)
- [ ] ردیابی سناریو: پرداخت VIP → کیف ۳۰م → درخواست وام ۲۰۰م
- [ ] رفع ثبت ناقص + نمایش در memberships / wallets / facilities
- [x] اکشن تأیید وام → هوک ساخت اقساط facility (اشتراک با E)
- [ ] badge داشبورد + QA نقش‌ها + E2E

### فاز G — شفاف‌سازی پورسانت (۰۲/۰۳؛ اولویت پایین‌تر طبق ۰۲/۰۵)
- [ ] ستون مبنا/مبلغ پایه در commissions (+ راهنما visitors)
- [ ] قفل قانون با کارفرما؛ تغییر فرمول فقط بعد از قفل

### فاز H — تست و پذیرش
- [ ] تمرکز اول: دو مورد اصلی ۰۲/۰۵ (فرانشیز٪ + اقساط درست)
- [ ] سپس VIP ادمین / پورسانت / ممیزی منو در صورت نیاز
- [ ] QA وب + `/app` (+ pastour)
- [ ] تیک پذیرش کارفرما

---

## وضعیت برآورد و اجرا

### برآورد (مستند)
- [x] آپدیت ۰۱ — ممیزی ادمین
- [x] آپدیت ۰۲ — فرانشیز٪ (+ مثال‌های ۱۰٪/۲۰٪ و اسکرین مشاوره)
- [x] آپدیت ۰۳ — تأیید کاربری
- [x] آپدیت ۰۴ — شکایات ادمین
- [x] آپدیت ۰۵ — مبنای پورسانت (۰۲/۰۳)
- [x] آپدیت ۰۶ — VIP→ادمین (۰۲/۰۳)
- [x] آپدیت ۰۷ — اقساط (۰۲/۰۴+۰۵)
- [x] ممیزی کد در برابر ۰۲/۰۵ (جدول انجام/مانده) در همین فایل ثبت شد

### اجرای Next (v4)
- [x] فاز B — فرانشیز٪ مدل
- [x] فاز C — تأیید کاربری
- [x] فاز D — اعمال٪ روی ویزیت (منطق + UI؛ pastour جزئی)
- [x] فاز E — اقساط صحیح (core)
- [ ] فاز F — VIP/وام ادمین (حداقل facility هوک شده؛ بقیه باز)
- [ ] فاز A / G — در صورت نیاز پذیرش
- [ ] فاز H — QA نهایی
- [ ] parity pastour کامل

---

## یادداشت فنی

- مسیرهای ادمین مرتبط: `insurances`, `complaints`, `reviews`, `help`, `installments`, `memberships`, `facilities`, `wallets`, `commissions`, `visitors`, `patients`, `access`.
- **اجرا شده (B–E):** `franchisePercent` + `payableFromFranchise`؛ `/admin/patients`؛ ConfirmPayment و مشاوره با ٪؛ توقف اقساط عضویت؛ `credit`/`facility` + سررسید شمسی.
- باقی‌مانده اولویت پایین‌تر: F کامل، A، G، pastour parity فرانشیز٪، QA دستی کارفرما.
- معیار قیمت نمونه: `visitFee` / `priceNum` ۳۵۰هزار؛ QA: ۱۰٪→۳۵هزار، ۲۰٪→۷۰هزار.
